# AATS TASKBOARD — Solana Meme-Coin Ultra-Sniper

**Single source of truth.** An agent works only on a task assigned to it here.
**Authoritative scope:** `.agency/00-brief/BUILD-DIRECTIVE-v3.md` + `AATS-BRIEF.md` + `CLAUDE.md §8`.
**Roster:** `.agency/04-plan/AATS-ROSTER.md` — use the SPECIALIZED agent, never the generic counterpart.
**Edge verdict:** `GO-PAPER-ONLY` (P0 DONE) — real capital DISABLED by default behind the DRY-RUN flag;
13 blocking conditions C-1..C-13 from `EDGE-VERDICT.md` flow into G0/G1/build/G4.

_Last updated: 2026-06-17 by `orchestrator` (**✅ ENHANCEMENT PROGRAM E1–E13 COMPLETE — `05-reports/gates/ENH-COMPLETE.md`.
THE WHOLE ENGAGEMENT IS DONE: core build G0–G6 ACCEPTED + the E1–E13 enhancement program CLOSED.** Close-out ledger across all 4 waves
(verified from the ACTUAL files, not the handoff/review JSON): **13/13 enhancements + 4/4 audits cleared — 16 ADDED · 1 COVERED (E8) · 0 FAILED.**
Every dual-G3-gated item PASS; no regression to the accepted core. **WAVE 4 (final) = 5/5 ADDED:** **E3** candidate-queue `GET /api/candidates`
+ dashboard (read-only, GET-only, no win-rate, `model_p` is a probability not money) · **E11** wallet-cluster `GET /api/wallet-cluster` + "Bubble Maps"
SVG page (projection of EXISTING detection — no new logic; bps int, scores non-money floats) · **E7** Narrative & News dashboard page
(`mcs_delta`∈[-1,0] re-clamped in the adapter — hostile frame → 0, never a buy) · **DEF-E10-01** the `python -O` de-risk-guard fix
(bare `assert` → `raise ValueError` + clamp; forged negative penalty now RAISES, mutation-proven 5/7 RED on revert).
**Consolidated suite GREEN — Python 2283 passed / 2 skipped (121.74s); dashboard build GREEN + 123 dashboard tests passed** (PYTHONHASHSEED=0, purged, no:cacheprovider).
**Security re-audit = PASS** (`05-reports/security/ENH-security-reaudit.md` — no new CRITICAL/HIGH; secrets sweep CLEAN; E1/E4/E3/E11 SAFE;
720-combo prompt-injection clamp HOLDS). **HARD RULES HOLD program-wide:** de-risk/selectivity/visibility-only (buy-trigger/size-up/stop-widen/leverage
INEXPRESSIBLE BY TYPE; E3/E7/E11 surfaces are read-only GET-only — zero control action), safety primitives (breaker/survivable-stop/DMS) UNTOUCHED + present,
point-in-time event-time-only, money int/Decimal, NO win-rate anywhere, no secrets, `aats/contracts` + `docker-compose.yml` NOT edited (sole frozen delta =
additive ADR-0013 `SubmitMode.DEVNET`). **REAL CAPITAL STAYS DRY-RUN-DISABLED + UNREACHABLE — the R3 pre-live checklist (Block A edge-on-RECORDED-data ·
Block B custody/security COND-G4-2 · Block C CEO legal+funding+sign-off) remains the gate before `DRY_RUN_ENABLED=false` and is NOT cleared by this program.**
**NO further dispatches — the engagement is closed.** Prior:
**ENHANCEMENT WAVE 3 VERIFIED + E1 RE-FIX FIXED — `05-reports/gates/ENH-wave3.md`.
WAVE 3 = 4/4 ADDED (dual G3 PASS): E6 (Discord ingestion) · E7 (News/breaking-news) · E9 (alpha-caller track-record) · E10 (social-velocity/bot-ratio).
E1 (devnet live-send) = DONE — FIXED (dual G3 PASS): the confirm/reconcile/idempotency BLOCKER is closed.** Verified by reading the ACTUAL changed
files under `C:/dev/aats` (not the handoff/review JSON). **E1 re-fix:** `reconcile()` gates `landed=True` on `submitted AND signature AND reason in
("landed","devnet_landed")` (`jito_jupiter_venue.py:1004-1006`) → unconfirmed devnet tx reconciles `landed=False`, RETRYABLE (idempotency keyed on
`fill.landed` `:342-343`); autouse env-snapshot fixture (`tests/execution/conftest.py:55-89`) HARD-CLOSES the DRY_RUN→LIVE leak (env-leak flake GONE,
201/2 ×6 PYTHONHASHSEED + 8 consecutive + 5x hostile `DRY_RUN_ENABLED=false`); confirm-timeout env-configurable (`:755-765`). Mutation-proven
(revert confirm-gate → regression RED 1/200). **WAVE 3 signals — ALL slow-loop-only, de-risk/selectivity-only (buy-trigger/size-up/stop-widen
INEXPRESSIBLE BY TYPE), point-in-time (shift-back leak-proven), injection-safe (quoted-untrusted / metadata-only):** E6 shill 0.191<organic 0.418
(NO FAST-path importer, 20 posts→1 LLM call); E7 `NewsSignal.mcs_delta`∈[-1,0] → credible-negative FORCE_EXIT/VETO_ENTRY via `DeRiskIntentFactory`;
E9 caller selectivity weight [0,1], NO win-rate (3/3 mutants KILLED, leak-free, deferred wiring→T-401); E10 `mcs_penalty>=0` bot-velocity penalty,
ships DORMANT. **CARRY (NON-BLOCKING):** DEF-E10-01 (MAJOR — bare-`assert` de-risk guard stripped under `python -O`; clamp-without-assert before E10
wires into live MCS @T-313/G4) + MINOR doc/cleanup (E6 F1/F2 `DISCORD_ALLOWLIST`→`DISCORD_CHANNEL_ALLOWLIST`, E9 unused `field`, E10 docstrings).
**HARD RULES hold:** safety primitives UNTOUCHED + present (`circuit_breaker.py:267`/`deadman.py:98`/`survivable_stop_coordinator.py:41`); real
capital DRY-RUN-disabled (`.env.example` `DRY_RUN_ENABLED=true` default L27); no secrets (placeholders only); money int/Decimal; `aats/contracts` +
`docker-compose.yml` NOT edited. **Consolidated suite GREEN — 2245 passed / 2 skipped (PYTHONHASHSEED=0, purged, no:cacheprovider).**
**NEXT → ENHANCEMENT WAVE 4 = E3 (candidate-queue) · E11 (wallet-cluster map) · the deferred E7 Narrative-and-News dashboard page;** then a FINAL
security/deploy/consolidated-suite re-verification of E1/E4/E5. Real capital stays DRY-RUN-disabled. Prior:
**ENHANCEMENT WAVE 2 VERIFIED — `05-reports/gates/ENH-wave2.md`.
8/8 items CLEARED dual G3: E8 COVERED · E2 · E13 · E12 · AUDIT-ratchet · AUDIT-micropreset · AUDIT-liquidity · AUDIT-risktiers ADDED.
**E2** (`risk/resting_orders.py`) ADDED — denylist wired as STEP-0 `:557-577` BEFORE the T-323 gate; `BuyFireRejection.DENYLISTED :214`;
`_DenylistLike` veto-only Protocol `:432`; denylisted Scam111 REJECTED pre-gate, builder never reached; mutation-meaningful (neuter→Scam111 fires, 4/6 RED).
**E8** (`risk/screener.py`, pre-existing 586L) COVERED — SLOW-loop PASS/REJECT-only survivor screen; `.env.example` E8 block `291-330`; no code change (verify-not-patch).
**E13** (`risk/anti_fomo.py`) ADDED — already-pumped(>300%)/mainstream-mention EXCLUSION; `conviction_multiplier` clamped `[0,1]` (`:439`, `>1` raises), REJECT→0/PASS→1; 33 tests.
**E12** (`risk/exit_engine.py`) ADDED — `STALE_NARRATIVE_TIME_STOP` (flat ≥N event-time hours AND narrative cooled → full SECURE exit); disabled-by-default; 22 tests.
**AUDIT-ratchet** (`exit_engine.py`) ADDED — discrete profit-lock `RATCHET_STOP :865-874` (breakeven@2x, lock 2x@3x), `locked_floor_r` monotone non-decreasing, 18 tests.
**AUDIT-micropreset** (`risk/presets.py`) ADDED — named MICRO preset (verified+LP≥30d+honeypot+top-5<20%+0.5%+18%-stop/24h); tighten-only composition; 37 tests.
**AUDIT-liquidity** (`risk/liquidity_sanity.py`) ADDED — pre-trade VETO: 24h-vol≥10x notional + x*y=k slippage sim ≤300bps; 31 tests.
**AUDIT-risktiers** (`risk/circuit_breaker.py` soft −2% REDUCE/PAUSE tier strictly below the hard trip + `models/gate_b.py` min-sample guard, default 30, floor 10).
HARD RULES upheld for every filter: **de-risk/selectivity-only** (no buy trigger, risk-increase type-inexpressible), **slow-loop-only** where applicable
(E2/liquidity are FAST-path VETOes by design — no social/news on hot path), **point-in-time** (event-time only, shift-back-one-tick leak-proven),
safety primitives (breaker/survivable-stop/DMS) UNTOUCHED + green, **real capital DRY-RUN-disabled**, money int/Decimal, no secrets,
`aats/contracts` + `docker-compose.yml` NOT edited. Full `tests/risk` 529 passed at end of wave (+185 over the 344 baseline). **E1** (devnet live-send re-fix)
**FAILED — engineer DIED again** (2nd consecutive death; PROCESS event, NOT a content strike — attempt count UNCHANGED). BLOCKER still live
(`jito_jupiter_venue.py:766-772/943/977` → unconfirmed tx reconciles `landed=True reason="filled" land_slot=None`). Architect issued **ADR-0013
`SubmitMode.DEVNET`** (additive, devnet-cluster-bound, structurally CANNOT unlock mainnet LIVE) — ACCEPTED; gives the re-fix a legal contract to build to.
Re-entry = scoped FIX on the confirm/reconcile/idempotency seam + unconfirmed-branch test FIRST → `solana-execution-engineer`, runs ∥ Wave 3.
**SAFETY HOLDS:** DRY-RUN default, mainnet LIVE 3-gated, devnet=worthless-SOL, primitives untouched, no secrets — real capital DISABLED + unreachable.
**Consolidated suite NOT run by orchestrator (no shell — Runtime must run the deterministic PYTHONHASHSEED=0 command to refresh the count with the
+185 Wave-2 tests; expect E1 `tests/execution/` instability until the E1 BLOCKER is fixed).** **NEXT → WAVE 3 = E6 (Discord) · E7 (News/breaking-news) ·
E9 (alpha-caller track-record scoring) · E10 (social-velocity/bot-ratio)** — slow-loop, adversarial-by-default, none a standalone buy trigger (E1 re-fix runs ∥). Prior: **GATE G6 — ACCEPTANCE → ACCEPTED
(agency-autonomous per `AUTONOMY-DIRECTIVE.md`; `G6-ACCEPTED.md`). CORE BUILD G0–G6 COMPLETE.** T-600 **DONE** (`DELIVERY.md` + `HONEST-EDGE-REPORT.md` client-ready + honest; README refreshed 1803→1842;
dual G3 PASS). All 5 BRIEF §4 acceptance criteria MET (paper end-to-end vs SimulationVenue + honest net-of-cost PnL/model-vs-baseline ·
daily-loss breaker + survivable stop + DMS PROVEN-BY-FIRING · pre-trade gate rejects honeypot/rug in sim · ONE `docker compose up` +
`.env.example` + monitoring/alerting live · README + deploy/ops + kill-switch runbook). HARD RULES hold: **NO win-rate anywhere**, edge
honestly **`UNPROVEN-NO-REAL-DATA` (GO-PAPER-ONLY)**, **real capital DISABLED** behind DRY-RUN + unreachable, money int/Decimal, no secrets.
Suite GREEN **1842/2/0**; T-402 e2e **16 passed**; `docker compose config` exit 0. **CARRIED (NOT a G6/PAPER blocker): PRE-LIVE (R3)
checklist** — Block A (edge on RECORDED data) = NOT MET, Block B (custody/security COND-G4-2: F-01 signer refusals + F-10 digests + F-07
host + F-02/03/04 supply-chain) = NOT MET, Block C (CEO legal + funding + R3 sign-off) = NOT GIVEN — all must clear before
`DRY_RUN_ENABLED=false`. **NEXT + FINAL: the E1–E13 enhancement program** (`ENHANCEMENT-DIRECTIVE-E1-E13.md` §ORDERING — runs LAST,
AUDIT-FIRST, additive; E1 Devnet live-send + E4 top priority). Real capital stays DRY-RUN-disabled. Prior: **GATE G5 — RELEASE → PASS
(`G5-PASS.md`).** T-500 **DONE** (one `docker compose up`,
11 services, config exit 0, DRY-RUN default, no secrets, colo plan honest) · T-501 **DONE** (docs/runbooks complete + honest:
no win-rate, edge UNPROVEN-NO-REAL-DATA, real capital DISABLED, R0-R4 ladder + pre-live checklist; BLOCKER R-501-01 false
verified-output fixed) · G4-fixes **DONE** (COND-G4-1 frozen-clock hermetic concurrent test + T-402-F1 breaker→StateStore
projection, both mutation-proven by two independent code-reviewers). Consolidated suite GREEN **1842/2/0** (concurrent test
now hermetic; 2 skips = solders-gated `test_tx_builder.py:161/:186`). **STAGE → G6 (Acceptance): T-600 delivery package +
CEO sign-off, then the E1-E13 enhancement program.** **Carried NON-blocking PAPER: COND-G4-2 = HARD R3/LIVE checklist**
(F-01 signer refusals + F-10 digests + F-07 host hardening + F-02/03/04 supply-chain) before `DRY_RUN_ENABLED=false`; Runtime
to re-run the suite once to refresh the recorded count; T-600 to refresh README §6 stale-prose 1803→1842. Real capital stays
DRY-RUN-disabled. Prior: **GATE G4 — INTEGRATION → PASS (conditional) (`G4-PASS.md`).**
T-400 **DONE (FINDINGS)** · T-401 **DONE (UNPROVEN-NO-REAL-DATA — harness BUILT+CORRECT)** · T-402 **DONE (PASS)** ·
T-403 **DONE (FINDINGS — core PASS)**. Leak/clock guards proven NON-VACUOUS (all RAISE on planted leaks, re-run this
session; 214 leak/clock/gate tests PASS; join event-time-only; no `truth_*`/no `sniper_sim`). Edge harness BUILT +
COMPUTES CORRECTLY (source-verified `aats/models/gate_a.py` + `tests/validation/*`; right sign on model-wins/-loses
controls, declines→0, net-of-cost, deterministic, clean-room guard non-vacuous, purge load-bearing) — `edgeVerdict =
UNPROVEN-NO-REAL-DATA` is the CORRECT, honest, ACCEPTABLE PAPER outcome (no recorded mainnet data; all corpora
`is_bootstrap_not_real`; G4 does NOT require proven edge). e2e PAPER demo PASS (KILL flattens <2s from dashboard AND
Telegram, non-vacuity proven; MODE propagates; SSE real `provenance:live_controller` frame; breaker + L2 stop + L3 DMS
fire on demand; risk-increase/no-auth rejected; de-risk-only). Security core PASS (secrets / Telegram authz / DRY-RUN /
prompt-injection — size-up type-inexpressible; 452 tests). **CARRIED (NON-blocking for PAPER): COND-G4-1** non-hermetic
`test_concurrent_thousand_snipes_one_winner` (30s wall-clock lock TTL vs 1000-thread storm — production logic
defensible; → `agent-orchestration-engineer` freeze injectable clock) · **T-402-F1** breaker persisted only to own
store, not projected into StateStore (`snipe_loop.py:179`; observability/dual-source defect, in-process gate still
blocks; → `agent-orchestration-engineer`/`solana-systems-architect`). **COND-G4-2 = HARD R3/LIVE CHECKLIST (NOT a PAPER
blocker):** F-01 signer-side refusals are an unbuilt scaffold + F-10/F-07 image/host hardening + F-02/F-03/F-04
supply-chain — must be built + test-proven BEFORE `DRY_RUN_ENABLED=false`. Real capital stays DRY-RUN-disabled.
**STAGE → G5 (Release): T-500 (`docker compose up` topology + colo/RPC plan) → T-501 (docs/runbooks).** After G6, the
E1–E13 enhancement program (`ENHANCEMENT-DIRECTIVE-E1-E13.md` §ORDERING). Prior: **SUITE STABILIZATION FINAL (`G3-stabilization.md`).**
**STAB-ingestion → DONE** (dual G3 PASS, test-layer only, source-confirmed): the cross-test global
event-loop pollution (pytest-asyncio 1.4.0 restored `set_event_loop(None)` after `tests/control_plane`,
breaking ingestion's synchronous `get_event_loop().run_until_complete()`) is FIXED — all 94 call sites →
`asyncio.run()` + defensive conftest; no-leak guards proven still-RED-under-mutation. **STABILITY GATE NOW
RUN — PASS: the consolidated suite is PROVEN STABLE — 1803 passed / 2 skipped / 0 failed, bit-for-bit
identical across 10 consecutive deterministic runs** (PYTHONHASHSEED=0, `-p no:cacheprovider`, `__pycache__`
purged each run; the only non-pass = the 2 allowed solders-gated execution skips at
`tests/execution/test_tx_builder.py:161/:186`, verified with `-rs`). **The consolidated suite is now
reliably green and PROVEN STABLE.**
**T-326 STILL NOT closed (att.3 landed + flake-resolved, dual-G3 VERDICT MISSING):** the att.3 PRODUCTION fix
(`sizing.py:89` `_hermetic_decimal_ctx()` + `:288` pinned prec=28/ROUND_HALF_EVEN inside
`FractionalKellySizer.size` — the real flake source: process-global decimal context fed the within-tick
aggregate clamp) IS in the tree, and the 10x gate (which exercises `tests/risk` incl. the new
`test_sizing_hermetic_under_hostile_decimal_context` covering test — the +1: 1803 vs 1802) is GREEN, so the
~1/50 att.2 flake is EMPIRICALLY ELIMINATED. BUT the att.3 re-dispatch **DIED** (`status:FAILED,
reason:"engineer died"`) before any `code-reviewer`+`backtest-qa` PASS on the safety-path diff. No verdict, no
progress (charter §3.4/§4) → **T-326 stays NEEDS-REPLAN; remaining work is VERDICT-ONLY** (re-dispatch dual G3
on the already-landed diff; flake-proof = this 10x gate). OFF milestone path, runs ∥ G4. Prior:
**Wave D re-spin + Wave E VERIFIED from source.**
**6 of 7 dual G3 PASS → DONE; 1 NEEDS-REPLAN (test-hardening att.3).** DONE: **T-340** controller integration
re-spin (R-1 prod fix `entry*0.60` -40% stop; BLK-2 mark=7 isolates `emergency_flatten:`; FIX-2 `ExitConfig
hard_stop_r=0.10` isolates `stop_exit:` — all 3 mutation-proven RED; dual PASS) · **T-341** control-plane re-spin
(widen-trap lex≠numeric `"10.0"<"2.5"` lex but 10.0>2.5 — `_coerce`-neuter → 4 RED; R-3 POST status codes; prod
unchanged; dual PASS) · **T-328** multi-wallet anti-cluster (activation gate load-bearing N_max=1, gate-defeat
mutant → 2 RED; dual PASS) · **T-301** enrichment + T-300a pending-table fix (`append_pending_row`/`read_pending_as_of`,
`read_as_of('pending_events')` → guided ValueError not KeyError; 4-mutation leak audit; dual PASS) · **T-302**
copy-trade SELECTIVITY stream (`smart_wallets_in` count-only never a trigger, honest lag ≥0, disabled-by-default;
dual PASS) · **T-303** completeness C-6 (Wilson-bounded miss rate, CENSORED-never-dropped, survivorship mutation →
8 RED; dual PASS; M-1 Wilson-z hardening → G4 non-blocking). **NEEDS-REPLAN (att.3, TEST-HARDENING, prod fix
CORRECT — OFF milestone path):** **T-326** code-reviewer BLOCKER B2 — the 3 new B1 aggregate-cap regression tests
FLAKE ~1/26 under full `tests/risk`, reproducing the exact 0.75-SOL-vs-0.5-SOL breach they guard (cross-test state
pollution; production within-tick accounting correct in isolation, 500/500 clean); backtest-qa PASS → dual FAIL on
the BLOCKER. `05-reports/gates/G3-waveE.md` recorded.
**✅ MILESTONE ACHIEVED: "the bot runs end-to-end on SimulationVenue (paper) and is driveable via the control-plane
(dashboard + Telegram), de-risk-only, DRY-RUN" — T-340 + T-341 both cleared dual G3.** Real capital DISABLED
(DRY-RUN); LIVE EDGE still UNPROVEN, gated at G4. **P3 BUILD LANES COMPLETE** (T-326 test-hardening runs ∥ G4, OFF
milestone path). **NEXT STAGE → G4 INTEGRATION:** T-400 (leak/clock audit C-5 + sim/paper burn-in + purged/embargoed
walk-forward + group-purge C-10) → T-401 (edge-vs-baseline GATE-A + GATE-B on recorded data, lower-95% bound>0,
survivor-MCS re-validation) → T-402 (e2e PAPER operator demo via dashboard + Telegram) ∥ T-403 (security/custody/
prompt-injection audit). Full-suite `pytest tests/ -q` count is a RUNTIME action (purge `__pycache__`). Prior wave:
`05-reports/gates/G3-waveD.md`._

---

## 0. Legend

- **Status:** `TODO | IN-PROGRESS | IN-REVIEW | DONE | BLOCKED`
- **Gate:** the gate this task feeds or is verified by (G0 scope · G1 arch · G2 design · G3 per-task build · G4 integration · G5 release · G6 acceptance).
- **Module:** M1 sensors · M2 engine · M3 controller · M4 guardrails · M5 immunity · `—` non-module.
- **Lane (P3 build):** A=ingestion/features/NLP · B=ML/LLM · C=execution/risk/MEV · D=controller+control-plane · E=operator dashboard · F=Telegram.
- **G3 is DUAL on every code task:** `code-reviewer` (apply AATS review brief, ROSTER §5) **AND** `backtest-qa-engineer` must both PASS. No verdict, no progress.
- **Safety-first ordering (HARD RULE):** the daily-loss circuit breaker, survivable stop, and dead-man's switch (T-310/T-311/T-312) are built and proven BEFORE any live-capable execution path is enabled.

---

## 1. Master task table

| ID | Title | Agent | Module / Lane | Depends on | Gate | Status | Attempts |
|---|---|---|---|---|---|---|---|
| **— WAVE P0: PRE-G0 EDGE GATE —** |
| T-000 | Edge verdict GO/NO-GO + success metrics + 13 conditions (C-1..C-13) | `quant-research-lead` | — | — | pre-G0 | DONE | 1 |
| **— WAVE P1: SPEC (G0) —** |
| T-100 | SPEC: numbered FR/NFR for BOTH halves (trading + operator UI), measurable acceptance criteria, scope/non-goals | `quant-product-analyst` | — | T-000 | G0 | DONE | 1 |
| T-101 | Encode GATE-A (net-of-cost PnL) + GATE-B (model-vs-naive-baseline) + HONESTY CLAUSE verbatim as acceptance criteria; no win-rate criterion anywhere | `quant-product-analyst` | — | T-000 | G0 | DONE | 1 |
| T-102 | Encode C-1..C-13 as numbered, testable G0 acceptance criteria (latency-honesty, frozen baseline, clean-room harness, group-purge, experiment-log, calibrated-haircut, staleness) | `quant-product-analyst` | — | T-000 | G0 | DONE | 1 |
| T-103 | Competitive-feature acceptance criteria: auto-sniper, migration sniper, copy-trade selectivity, limit/DCA, TP-ladder+trailing, multi-wallet/bundle, MEV fast/secure, token-safety scanner, discovery enrichment, P&L cards, Telegram — each measurable + mapped to a lane | `quant-product-analyst` | — | T-000 | G0 | DONE | 1 |
| T-104 | Operator-UI + Telegram acceptance criteria: every control drives the real bot; kill flattens within budget; live feed reflects real events; commands DE-RISK only | `quant-product-analyst` | — | T-000 | G0 | DONE | 1 |
| T-105 | Capital-staging gates R0..R4 as acceptance criteria; real capital CEO-gated at R3; staged-rollout (sim→shadow→paper→tiny-real→scale) | `quant-product-analyst` | — | T-000 | G0 | DONE | 1 |
| T-106 | Apply G0 traceability fixes F-1a/F-1b (SPEC §9 mislabeled migration + P&L-card AC ids) + F-2 (US-006 editorial note) — cosmetic, before architect consumes spec | `quant-product-analyst` | — | T-100 | G0 | TODO | 0 |
| **GATE G0 — Scope.** | | | | T-100..T-105 | G0 | **APPROVED (agency-autonomous per AUTONOMY-DIRECTIVE.md)** — record: `05-reports/gates/G0-APPROVED.md`; all 10 OQ defaults adopted | — |
| **— WAVE P2: ARCHITECTURE (G1) — NO CODE BEFORE CEO APPROVES G1 —** |
| T-200 | BLUEPRINT: promote `./sol-sniper` seams to production; triple-loop topology; Rust-hot-path / Python boundary; message bus (Redis Streams); typed contracts; ADRs | `solana-systems-architect` | — | G0 | G1 | DONE | 1 |
| T-201 | FREEZE the control-plane API contract (`/state /feed /kill /flatten /flatten/{mint} /breaker/reset /mode /metrics /positions /latency /sentiment /predictions /reasoning /risk-config /health`) — ONE contract that server (D), dashboard (E), Telegram (F) all build to; reconcile with `dashboard/src/lib/api.ts` ENDPOINTS | `solana-systems-architect` | — | G0 | G1 | DONE | 1 |
| T-202 | Pluggable `ExecutionVenue` iface + pluggable program-ID/venue registry (pump.fun + PumpSwap + Raydium v4 + CPMM + Meteora/Moonshot candidates) — verified LIVE, never a stale ID in a hot path | `solana-systems-architect` | M4 | G0 | G1 | DONE | 1 |
| T-203 | Point-in-time feature store design + the Data Contract; add first-K-slot buy-pressure/volume feature so GATE-B baseline is CONSTRUCTIBLE (C-4) | `solana-systems-architect` | M1/M2 | G0 | G1 | DONE | 1 |
| T-204 | Clean-room recorded-data validation harness design + import/static-analysis guard that FAILS the build on any `truth_*` reference (C-7); R1/R2 cost stack must NOT inherit `_competitor_delay`/sandwich constants (C-2) | `solana-systems-architect` | — | G0, T-000 | G1 | DONE | 1 |
| T-205 | `latency-budget.md`: DETECTION-COMPETITIVE, SUBMISSION-DISADVANTAGED in plain numbers — internal compute split from block-engine RTT + staked-lane/colo gap; named extra-slot penalty propagated as `buyers_ahead`/adverse-selection (C-1) | `solana-systems-architect` (owns doc) + `mev-latency-engineer` (feeds ms/hop) | M4/M5 | G0 | G1 | DONE | 1 |
| T-206 | Docker-compose deploy topology: bot + control-plane API + dashboard + Telegram + Redis + Prometheus/Grafana/Alertmanager; DRY-RUN flag as architecture constraint | `solana-systems-architect` | M5 | G0 | G1 | DONE | 1 |
| **GATE G1 — Architecture.** | | | | T-200..T-206 | G1 | **APPROVED (agency-autonomous per AUTONOMY-DIRECTIVE.md)** — record: `05-reports/gates/G1-APPROVED.md`; contract FROZEN, C-1..C-13 structurally enforced, red-team blocksG1 (leak/custody) resolved by ADR-0009/0010; code may begin | — |
| **— WAVE P2.5 / WAVE F (FOUNDATION): SCAFFOLD + CONTRACTS + CUSTODY (parallel, G1 APPROVED) —** |
| T-199 | Shared typed contracts package `aats.contracts` (events/features/venue/intents/risk/positions/api-schemas/provenance) transcribed from FROZEN data-models.md + api-contracts.md + execution-venue.md; ADR-0006/0010 enforced by construction | `solana-systems-architect` | — | G1 | G3 | **CLOSED** (split T-199a/T-199b both DONE) | 2 |
| T-199a | RE-PLAN of T-199 (BLOCKER): root-cause + ELIMINATE the intermittent `extra="forbid"` leak-guard non-enforcement on `FeatureFrame`; prove a REPEATABLE green full suite under PYTHONHASHSEED sweep in CI; leak guard proven by mutation test | `solana-systems-architect` | — | G1 | G3 | **DONE** (dual G3 PASS — `features.py:170` extra=forbid + `:292` model_rebuild(force=True), no `__future__`; 180 contracts green across 14 PYTHONHASHSEED + 20-seed CoreSchema proof; forbid→ignore mutation drives 6 negatives RED) | 1 |
| T-199b | RE-PLAN of T-199 (MAJOR): `LatencyHop.cls` → `Field(alias="class", serialization_alias="class")` + `populate_by_name=True`; add wire-key schema test asserting `model_dump_json` emits `"class"` (frozen api-contracts.md §4 `/api/latency`) | `solana-systems-architect` | — | G1 | G3 | **DONE** (dual G3 PASS — `api_schemas.py:248` alias="class" + populate_by_name=True; `model_dump_json(by_alias=True)` emits `"class"`, `cls` absent; round-trip verified) | 1 |
| T-250 | Repo/Docker/Compose scaffold (dashboard + Telegram as services; **+ `aats-signer` + Vault wiring** per ADR-0009 delta); CI for Python + dashboard; Prometheus/Grafana/Alertmanager; promote `sniper_sim/metrics.py` to real telemetry the dashboard reads | `latency-devops-engineer` | M5 | G1 | G3 | **DONE** | 2 |
| T-251 | `custody-policy.md` + **implement `aats-signer` policy** (per ADR-0009 / infrastructure.md §5): separate minimal-surface signer process, 3 independent signer-side refusals (per-tx+aggregate SOL cap · full enumerated program allowlist · Jito-tip-account-pinned transfers), Vault short-lived token + `mlock` + zeroize secret handling, trade-only CAPPED non-custodial hot wallet, `.env.example` (Vault refs only), Telegram-command authz | `crypto-security-engineer` | M5 | G1 | G3 | **DONE** | 1 |
| T-352a | NEW (ADR-0009 delta) — `aats-signer` service: minimal-surface signer compose unit + loopback Unix-domain socket + file-permission/peer-credential hardening; `latency-devops-engineer` builds, `crypto-security-engineer` audits | `latency-devops-engineer` + `crypto-security-engineer` | M5 | G1, T-251 | G3 | READY | 0 |
| **— WAVE P3: BUILD LANES — ✅ COMPLETE (all milestone-path tasks dual G3 DONE; T-326 test-hardening att.3 runs ∥ G4, OFF milestone path) —** |
| **Lane A — M1 Sensors** |
| T-300 | Ingestion transport: Yellowstone/Geyser gRPC + ShredStream; pump.fun/PumpSwap/Raydium v4/CPMM decoders; Redis Streams bus; point-in-time store; **SHADOW/RECORD mode** for real first-K-slot training data (R1) | `data-ingestion-engineer` | M1 / A | T-202, T-203, T-250 | G3 | **DONE** (dual G3 PASS; 107 tests; no program-ID literal in hot path; store partitions on on-chain block_time; CENSORED-not-dropped. Live Geyser/WS are disclosed STUBs. 1 MAJOR filed → **T-300a**) | 1 |
| T-300a | **FIX (from T-300 G3, MAJOR):** `decoders.py:_make_event_time` substituted wall-clock into the AUTHORITATIVE on-chain `block_time_ms` anchor when `block_time_unix_s` is None → compute-time leak + staleness collapsed to 0. Gates C-5 clock-audit (T-400). | `data-ingestion-engineer` | M1 / A | T-300 | G3 | **DONE** (dual G3 PASS; `decoders.py:202` returns None when block_time absent — NO wall-clock substitution; STALENESS_UNKNOWN=-1 sentinel; all 9 `_try_*` guard None; `write_pending_slot_event` event_date=None/dataset='pending_events'; `EventTime(block_time_ms=0)` rejected. Leak-reintro mutation → 19/33 RED. **1 MAJOR filed → T-301** `read_as_of('pending_events')` KeyError; MINOR `_rows` private write) | 1 |
| T-301 | Discovery enrichment: DEXScreener / Birdeye / Meteora / Moonshot; venue/program-ID registry kept PLUGGABLE + verified LIVE. **+ ABSORB T-300a carry-forward:** dedicated `pending_events` Parquet table so `read_as_of('pending_events')` no longer raises `KeyError: 'mint'` on the mixed-shape pending row; public `append_pending_row` instead of `_parquet._rows` private write. | `data-ingestion-engineer` | M1 / A | T-300 | G3 | **DONE** (dual G3 PASS; 206 ingestion tests; injectable HttpTransport adapters, offline fixtures, None-on-error; **T-300a fix verified** `store.py:166` public `append_pending_row` + `:221` `read_pending_as_of` + `:204-209` `read_as_of('pending_events')`→guided ValueError NOT KeyError; pending quarantined in separate `_pending_rows` table (no `mint`, event_date=None). 4-mutation leak audit (as-of guard / cutoff / forged event_date / forward-dated anchor) all caught RED, byte-restored. Slow-path-only. **Live Meteora/Moonshot shapes + AiohttpTransport → T-400**) | 1 |
| T-302 | Smart-money / copy-trade wallet stream (`accounts_subscribe` on profitable wallets) — RECORD entry/exit timestamps vs our event-time for lag accounting (EH-005) | `data-ingestion-engineer` | M1 / A | T-300 | G3 | **DONE** (dual G3 PASS; 62 tests + 268 ingestion; DISABLED by default, 0-20 wallet cap; `smart_wallets_in` is a plain int COUNT never a buy trigger (public surface audited: only `count_smart_wallets_in()->int` faces consumers, NO EntryIntent/sizing/mirror); honest lag (`entry_lag_slots`/`observation_lag_ms` ≥0, on-chain `their_fill_slot` window not observation slot — 7 adversarial probes); None fill_block_time→None (T-300a law); money int, float→TypeError; injectable ReplaySmartWalletBackend. **EH-005 zero-lift + Geyser accountSubscribe stub → G4 T-401**) | 1 |
| T-303 | Recorded-data completeness audit hooks: reconcile against independent pool-create census; bound miss rate; censored/right-truncated outcomes never dropped (C-6) | `data-ingestion-engineer` | M1 / A | T-300 | G3 | **DONE** (dual G3 PASS; 65 tests + 333 ingestion; injectable `CensusSource` Protocol (no network); miss rate bounded via **Wilson CI upper bound** + gated vs `declared_max_miss_rate`; CENSORED-never-dropped (`coverage_fraction`≡1.0); survivorship-free. REAL impl mutation (`if status==CENSORED: continue`)→8 RED, byte-restored; Wilson math verified to 5dp; small-N fails CLOSED. **MINOR M-1:** `_wilson_upper_bound` silent-coerces off-list confidence to z=1.96 — default/documented paths correct → **G4/hardening fix** non-blocking) | 1 |
| T-304 | Feature engineering: first-60s microstructure (LP, holders, sniper-cluster, tax) + survivor TA (RSI/MACD/BB) + `smart_wallets_in` feature; FeatureFrame assembly; point-in-time correctness | `feature-quant-engineer` | M1 / A | T-300, T-303 | G3 | **DONE** (dual G3 PASS, attempt 2; 177 tests; B1+M1 fixed — event_time/first_k_slots guards + final cutoff re-validation; lookahead refused at 3 layers; `smart_wallets_in` is a count-only feature, no buy trigger) | 2 |
| T-305 | Buy-pressure/volume first-K-slot feature (C-4 dependency) so the naive-momentum baseline is constructible | `feature-quant-engineer` | M1 / A | T-203, T-300 | G3 | **DONE** (dual G3 PASS, attempt 2; 48 tests + 228 regression; B-1+M-1 fixed — `classify_event_direction` raises on zero-reserve, sig now `Sequence[tuple[LaunchEvent,bool]]` w/ decode-time `is_buy`; both window bounds enforced; mutation-proven net≠gross. **API-notice → T-300/T-340**) | 2 |
| T-306 | MCS sentiment pipeline (Tier-A CryptoBERT filter + Tier-B batched LLM) — slow-loop ONLY, coordinated-shill → CONTRARIAN penalty; ingested text is quoted untrusted data | `nlp-sentiment-engineer` | M1 / A | T-300 | G3 | **DONE** (dual G3 PASS; 72 tests; `tier_b.py` `conviction = clamp(raw·(1−penalty),0,1)` multiplicative shill penalty drives manufactured euphoria → 0 (organic 0.53 vs shill 0.21); ONE LLM call/asset (20 posts→1); point-in-time filter is FIRST op; injection = quoted-untrusted data, OBEYED-injection cannot raise exposure; MCS bounded [0,1] by validator. **MINOR carry:** off-topic age-penalty inflation (de-risk dir only); `models.py` docstring says [-1,1] (impl/validator [0,1]) — doc fix, no ADR; add equal-volume AC-010 control test) | 1 |
| **Lane B — M2 Engine** |
| T-310 | Snipe classifier: LightGBM → ONNX, single-digit ms; leak-free labels (event-time); CALIBRATED probability + uncertainty (reliability curve required) | `ml-prediction-engineer` | M2 / B | T-304, T-305 | G3 | **DONE** (dual G3 PASS; 55 tests; labels joined by `event_time_key=(slot,block_time_ms)` ONLY (`training.py:106`, wall-clock excluded); `assert_event_time_leq_decision` + `assert_no_label_taint` build guards; forward-only split. **LOAD-BEARING leak proof:** label-as-feature → OOS AUC 1.0000 vs clean 0.7450; shuffled-label → ~0.5. Calibrated ECE 0.02–0.04, Brier≈0.15; ONNX parity 2.76e-07; latency p99 0.32ms tree. Monotone de-risk constraints REAL in tree. ALL BOOTSTRAP — live edge @G4) | 1 |
| T-311 | Naive-momentum baseline FROZEN in committed hashed config (K/percentile/unit-of-risk/universe) before any model training; test FAILS if it changes after first fit (C-4) | `ml-prediction-engineer` | M2 / B | T-305 | G3 | **DONE** (dual G3 PASS, attempt 2; 16 tests; `baseline.frozen.json` selection_percentile=60.0, frozen_hash=e1e7dc6c == live `canonical_params_hash` (CONSISTENT); layout-independent digest; money int/Decimal-as-string, float rejected; all 7 hashed params mutation-meaningful (post-fit change → `baseline_changed_after_fit` RED), non-hashed key inert; file now tracked, tested==committed bytes. `BaselineSignal` NO price/size/decision. Att.1 G3 found hash-inconsistent config + vacuous proof — both fixed att.2) | 2 |
| T-312 | Slow-loop TFT survivor model + model-vs-baseline monitor (the headline GATE-B delta surfaced to telemetry) | `ml-prediction-engineer` | M2 / B | T-310, T-311 | G3 | **DONE** (dual G3 PASS; 96 tests; `gate_b.py` delta = model−baseline net-PnL-per-SOL-at-risk on recorded `TradeOutcome`; lower-95% bootstrap bound>0 pass bar; empty→ValueError fail-closed; NO win-rate; gauge `aats_model_vs_baseline_delta_net_pnl_per_sol` (AC-037). Survivor leak-free (same construction; shuffled-label→0.47); ordered quantiles p10≤p50≤p90, NO price; slow-loop-only. **CARRY @T-400/401:** MCS covariates CONSTANT in bootstrap (no `mcs` on SyntheticRow) — de-risk wiring monotone-pinned but UNEXERCISED; re-validate on real MCSScore rows) | 1 |
| T-313 | Schema-enforced de-risk-only Reasoner + LLM router; sub-200ms veto path; asymmetric-trust clamp (may only VETO/force-exit, never size-up/widen-stop/override hard-stop); narrative is untrusted data | `llm-reasoning-engineer` | M2 / B | T-306, T-310 | G3 | **DONE** (dual G3 PASS; 64 tests; `ReasoningAction` EXACTLY {HOLD,VETO_ENTRY,REDUCE_SIZE,FORCE_EXIT} — size-up/widen/leverage/override-stop INEXPRESSIBLE BY TYPE + import-time static guard; clamp drops risk-increase tokens→HOLD+flag; `DeRiskIntentFactory` no entry method; veto p99≈0.07ms<200ms, timeout→conservative VETO; LLM off SNIPE (lazy imports); narrative quoted-untrusted, OBEYED-injection→HOLD+no Intent; point-in-time refuses mismatched event-times; contracts import-only. Clamp-invert mutation→4 tests RED) | 1 |
| **Lane C — M4 Guardrails (SAFETY-FIRST — these gate the live path)** |
| T-320 | **Daily-loss circuit breaker — HARD HALT** (trip at −X% / −0.30 SOL; stop entries, flatten/hand to survivable stop, manual re-arm; LLM may trip not reset) — **BUILT + PROVEN FIRST** | `risk-guardrails-engineer` | M4 / C | T-250 | G3 | **DONE** (dual G3 PASS; PROVEN-BY-FIRING; B1 fix + ADR-0012) | 2 |
| T-321 | **Survivable stop** (venue-native resting order + in-process FAST-loop enforcer + dead-man's switch) — stop holds even if bot process dies — **BUILT + PROVEN before any live path** | `risk-guardrails-engineer` (rule) + `solana-execution-engineer` (venue-native) + `agent-orchestration-engineer` (enforcer/DMS) | M4 / C | T-320 | G3 | **DONE** (dual G3 PASS; 3 layers PROVEN-BY-FIRING incl. process-death) | 1 |
| T-322 | **Dead-man's switch** (heartbeat loss → flatten/stop) backing the breaker | `risk-guardrails-engineer` | M4 / C | T-320 | G3 | **DONE** (dual G3 PASS; PROVEN-BY-FIRING on heartbeat-loss) | 2 |
| T-323 | Sub-10ms pre-trade safety gate (honeypot, mint/freeze authority, LP lock, dev/bundle cluster, holder concentration) — local checks in hot path; **token-safety scanner** red flags exported to dashboard | `risk-guardrails-engineer` | M4 / C | T-320, T-202 | G3 | **DONE** (dual G3 PASS, attempt 2; 165 tests; AC-011 corpus 20/20 rejection MEASURED w/ logged on-chain reason; p99≈60µs <10ms MEASURED; 0-IO/0-RPC/0-LLM; mutation-proven) | 2 |
| T-324 | Hierarchical risk rule engine + fractional-Kelly sizing (≤ ¼ Kelly, never full; signals may only SHRINK); cost-aware entry gate (`expected_edge_bps > total_cost_bps` or NO TRADE) | `risk-guardrails-engineer` | M4 / C | T-320, T-323 | G3 | **DONE** (dual G3 PASS; 237 tests; cost gate rejects iff edge≤cost incl. ties; ≤¼ Kelly binds P 0.1-0.9; no signal sizes up (20k fuzz); asymmetric LLM trust; event-time idempotency; 150bps haircut widen-only) | 1 |
| T-325 | TP-ladder + trailing-stop + hard-stop + timeout ExitEngine productionized (Photon priority #1, biggest proven lever) + "auto-strat" presets | `risk-guardrails-engineer` | M4 / C | T-321 | G3 | **DONE** (dual G3 PASS; 285 tests; Secure default OQ-008; trailing tightens-never-widens (peak_r ratchet, mutation-proven); de-risk only; hard stop == T-321 StopState; A/B beats naive +25% (not RNG artifact). Per-task only — NOT G4 edge-vs-baseline) | 1 |
| T-326 | Limit + DCA resting orders that fire when operator is OFFLINE — de-risk-direction only | `risk-guardrails-engineer` | M4 / C | T-321, T-325 | G3 | **NEEDS-REPLAN — att.3 LANDED + FLAKE-RESOLVED, dual-G3 VERDICT MISSING** (the att.3 PRODUCTION fix IS in the tree — `sizing.py:89` `_hermetic_decimal_ctx()` + `:288` `FractionalKellySizer.size()` runs all Decimal math inside a pinned prec=28/ROUND_HALF_EVEN context, the TRUE flake root cause = process-global decimal context fed the within-tick aggregate clamp, exactly the att.2 backtest-qa BLOCKER; new `test_sizing_hermetic_under_hostile_decimal_context` asserts cap-holds + byte-identical across 63 hostile contexts. **STABILITY GATE NOW GREEN:** the 10x consolidated gate (1803/2/0 ×10) exercises `tests/risk` incl. this covering test (+1: 1803 vs 1802) and the att.2 ~1/50 flake did NOT reproduce — flake EMPIRICALLY ELIMINATED. **STILL NOT DONE:** the att.3 re-dispatch DIED (`status:FAILED, reason:"engineer died"`) before any `code-reviewer`+`backtest-qa` PASS on this safety-path prod diff — no verdict, no progress (charter §3.4/§4, overlay rule 3). The death is a PROCESS event, NOT the 3rd content strike. **Re-entry = VERDICT-ONLY:** re-dispatch dual G3 on the already-landed `aats/risk/sizing.py` + `tests/risk/test_resting_orders.py` diff (backtest-qa may cite this 10x gate as the stability proof; confirm the hermetic context is mutation-meaningful + the hostile-context test binds the cap). Dual PASS → DONE. If att.3 fails dual G3 → 3rd content strike → CEO escalation (charter §3.5). OFF milestone path, runs ∥ G4. See `05-reports/gates/G3-stabilization.md`) | 3 |
| T-327 | Real `JitoJupiterVenue` behind the seam — direct-AMM snipe buy + Jupiter v6/Ultra exits — **DRY-RUN / no-submit FIRST** (quote→build→sign→DON'T-send); `simulateTransaction`; retries | `solana-execution-engineer` | M4 / C | T-202, T-321 | G3 | **DONE** (dual G3 PASS, attempt 2; 75 pass+2 solders-skip; **DRY-RUN no-submit PROVEN** — triple gate, land() short-circuits before any network call, mutation-proven; B1/B2/B3 fixed — no program-ID literals fail-CLOSED, sim-bytes==land-bytes, fresh-blockhash retry; `sign()` via UDS ADR-0009. Pre-LIVE: re-run 2 solders skips) | 2 |
| T-328 | Multi-wallet / bundle execution + partial fills + anti-cluster (blast-radius caps) | `solana-execution-engineer` | M4 / C | T-327, T-251 | G3 | **DONE** (dual G3 PASS, att.2; 45 tests + 171 execution; **QA-MAJOR-1 test-only fix** — activation gate (OQ-010/R3) now load-bearing: `N_WALLETS_MAX=3` w/o `N_WALLETS_MAX_ENABLED`→`_resolve_n_max(None)==1`; 2-wallet construct w/o flag→`MultiWalletConfigError`; gate-defeat mutant (`if False and …` `multi_wallet.py:592`)→ exactly 2 RED, others green, byte-restored. N_max default 1; anti-cluster cap=per-trade w/ pre-exec refusal (`execute_count==0`); DRY-RUN no-submit (`rpc.send_calls==0`); key via UDS signer ADR-0009; money int float→TypeError; ExecutionVenue ABC preserved. Prod unchanged. NIT: unused `field` import `:38`) | 2 |
| T-329 | Honeypot/sellability simulation primitive (sell-sim) the risk gate calls pre-trade | `solana-execution-engineer` | M4 / C | T-327, T-323 | G3 | **DONE** (dual G3 PASS; 51 tests; `sell_sim.py` DRY-RUN by construction (no `send_transaction` path), refuse-by-default, simulate-before-sign; honeypot fingerprints: revert / zero-out / sell-tax>thresh / signer-refusal / infra-err; satisfies `pretrade_gate.SellSimProbe`, no-probe still refuses; risk gate de-risks RedFlag.NOT_SELLABLE. 6 production branches + 2 gate-wiring mutants KILLED; money int/Decimal, off hot path (N+2)) | 1 |
| T-330 | Latency budget instrumentation per hop; Jito bundles + edge-bounded dynamic tips (read LIVE `tip_stream`, bound by 0.30× edge, never hardcode); atomic buy-with-revert | `mev-latency-engineer` | M4 / C | T-327, T-205 | G3 | **DONE** (dual G3 PASS; 46 tests; `tip_controller.py:120` ceiling=floor(0.30×edge) exact Decimal, reads LIVE injectable tip floor point-in-time (never hardcoded), `DO_NOT_SUBMIT` when floor>ceiling / no-edge / no-floor; logs tip-%-of-edge bps. C-1 three-class ledger (INTERNAL/SUBMISSION/LEADER_LAND) never summed; atomic buy-with-revert (`BundleResult` forbids landed_buy w/o LANDED); DRY-RUN default. No inherited sim optimism C-2. Tip-bound + ledger-split mutations → 4–9 tests RED) | 1 |
| T-331 | MEV protection: private split exits; **fast vs secure** modes; sandwich avoidance; log live tip floor at decision time per candidate for tip-contention stratification (C-3) | `mev-latency-engineer` | M4 / C | T-330, T-325 | G3 | **DONE** (dual G3 PASS; 26 tests; `split_exit.py:469` ASYMMETRY invariant structural — refuses plan where modeled_sandwich>FAST baseline; every leg private; legs sum exact int-bps. C-3: `TipContentionRecorder` stamps live floor + ContentionBucket {LOW/MED/HIGH/PRICED_OUT} per candidate to GATE-A sink; PRICED_OUT cohort recorded; point-in-time `as_of_slot≤decision_slot` at construction. SECURE default (OQ-008); asymmetry+lookahead+exposure-gap mutations KILLED. **MINOR carry:** add leg-sum negative test + comment on redundant clamp; modeled priors re-modeled @T-401) | 1 |
| **Lane D — M3 Controller** |
| T-340 | Triple loop (SNIPE/FAST/SLOW) + single-writer per-position FSM + atomic snipe→fast handoff (write-ahead, intent dedup); shared Redis state; runs vs SimulationVenue FIRST | `agent-orchestration-engineer` | M3 / D | T-310, T-313, T-324, T-327 | G3 | **DONE** (dual G3 PASS, att.3; **R-1 prod fix** `snipe_loop.py:312-325` `hard_stop=floor(entry*Decimal("6")/Decimal("10"))=entry*0.60` (-40% stop BELOW entry; was `entry*6`=above-entry immediate-halt) PINNED by `test_…sixty_percent_of_entry` (revert→`30==3` RED); audit-only field, never read to fire. **BLK-2 FIXED** `test_snipe_handoff.py:462` mark 0→7 (>6.0 threshold) isolates breaker→`emergency_flatten:` (no-op `emergency_flatten_all`→`exit_calls=[]` RED, AC-028 PROVEN). **FIX-2** `test_t342_enforcer.py:1213` `ExitConfig hard_stop_r=0.10` keeps ExitEngine silent at 59 so only Layer-2 `stop_exit:` fires (no-op `on_tick`→`exits=[]` RED). 110 controller + 395 controller+risk green; ruff/mypy clean. CI-flake re-check → G4) | 3 |
| T-341 | Control-plane API server conforming EXACTLY to the FROZEN P2 contract (T-201); drives kill/flatten/breaker-reset/mode against the real bot; SSE `/feed` of real events | `agent-orchestration-engineer` | M3 / D | T-201, T-340 | G3 | **DONE** (dual G3 PASS, att.3; **QA-MAJOR-1 FIXED** test-only — `test_widen_trap.py` 8 unit tests on `_validate_risk_config_tighten_only` using lex≠numeric trap (`"10.0"<"2.5"` lex but `10.0>2.5`; both `daily_loss_limit_pct` + `jito_tip_cap_frac`); neuter `_coerce` Decimal branch→ exactly 4 RED (mutation-proven by both reviewers). **R-3** 9 POST status-code conformance tests vs api-contracts §3/§5 (kill/flatten/{mint}→202, breaker/reset→200|409, mode→200|403, risk-config→200|403). **Prod `server.py` byte-unchanged** (`git diff --quiet` exit 0; `_coerce`/checks `:270-333` are existing correct impl). 112 control_plane green; ruff/mypy clean. NIT: 409-armed test precondition not asserted) | 3 |
| T-342 | In-process FAST-loop survivable-stop enforcer + dead-man's-switch heartbeat wiring (the OPERATES half of the survivable-stop seam) | `agent-orchestration-engineer` | M3 / D | T-321, T-322, T-340 | G3 | **DONE** (dual G3 PASS, att.2; B1 live-wiring `FastLoopEnforcerWiring` into `FastLoop.tick()`/`reconcile_fill`/exits, B2 `HeartbeatWriter` write-only Protocol no type:ignore, B3 RedisHeartbeatWriter tests, M1 first-beat age sentinel, M2 docstring — all fixed; process-death→DMS flatten PROVEN-BY-FIRING; AC-026 p99≈6.3ms@100pos, AC-027 PASS; no await/LLM/RPC on FAST path; 3 runtime mutations KILLED) | 2 |
| **Lane E — Operator dashboard (FINISH `./dashboard`, do NOT rebuild)** |
| T-350 | Close open review items: centralize `<Layout>`, shared chart-color module, fix stale events/min counter on SnipeFeed, clear ~17 ESLint advisories | `frontend-engineer` + `uiux-designer` | — / E | T-250 | G3 | **DONE** (code-reviewer PASS by own build+lint; UI surface, no backtest-qa needed) | 1 |
| T-351 | Vitest tests for destructive controls (kill / flatten / breaker-reset / mode) + key pages | `frontend-engineer` | — / E | T-350 | G3 | **DONE** (dual G3 PASS — code-reviewer PASS + backtest-qa 2nd PASS; 5 files/24 vitest tests; endpoints pinned to FROZEN contract; confirm-gating on kill+go-live; zero network in mock mode; 4 mutants caught. Unblocks T-352) | 1 |
| T-352 | WIRE dashboard to live control plane (`VITE_USE_MOCK=false`, live `/feed`) with mock as offline fallback; still builds GREEN on mock | `frontend-engineer` | — / E | T-341, T-351 | G3 | **DONE** (G3 PASS, att.2; UI — code-reviewer PASS, no backtest-qa needed; BLOCKER R-01 fixed: `api.ts:770` `setMode` sends canonical wire enum `toWireMode(m)` not lowercase view-model → no more 400; LIVE server-fenced 403 de-risk-only; tsc 0, 46/46 vitest, mock build GREEN, eslint clean) | 2 |
| T-353 | Surface every new feature: copy-trade page, resting orders, auto-strat presets (Settings), token-safety red flags, P&L cards + export | `frontend-engineer` + `uiux-designer` | — / E | T-352 | G3 | **DONE** (G3 PASS, att.1; UI — code-reviewer PASS; copy-trade SELECTIVITY page (no buy/mirror control, neg-test asserted), AutoStrat/RestingOrders configure EXITS only, RedFlags kit + SnipeFeed/Risk surfaces, Positions net-of-cost-PRIMARY P&L cards + export `pnl_basis=net_of_cost...`; NO win-rate; money via int-lamport/Decimal adapter; 3 new GETs quarantined in `OPTIONAL_ENDPOINTS` (frozen contract untouched); 74/74 vitest, build GREEN) | 1 |
| **Lane F — Telegram operator channel** |
| T-360 | Telegram alert channel: real-time fills, rugs-avoided, breaker trips — against the SAME control-plane contract (T-201) | `backend-engineer` | — / F | T-341 | G3 | **DONE** (dual G3 PASS, att.1; `aats/telegram/` outbound-only ALERT channel — 3 classes FILL/RUG_AVOIDED/BREAKER_TRIP (rising-edge once), consumes FROZEN SnipeEvent §6 via injectable FeedSource; cannot increase risk by construction; token env-only + redaction chokepoint + source-grep test; money int-lamports via Decimal, float→TypeError; no win-rate; 42 tests, 3 mutants KILLED) | 1 |
| T-361 | CONSTRAINED command set (status / kill / flatten / pause) that can ONLY DE-RISK; authz from `crypto-security-engineer` custody policy (T-251) | `backend-engineer` | — / F | T-360, T-251 | G3 | **DONE** (dual G3 PASS, att.1; EXACTLY `/status /kill /flatten <mint> /pause` vs FROZEN §7; de-risk-only STRUCTURAL — closed `_KNOWN_COMMANDS`, ControlPlaneClient Protocol = 4 read/de-risk methods only, `pause()` posts hard-coded SHADOW const (mode cannot move UP from Telegram); operator-ID allowlist gate-1 (empty→fail-closed, unauth→drop+fingerprint, ZERO CP calls); single-use TTL confirm nonce on kill/flatten; 86 tests, 22-verb risk-increase probe → ZERO CP calls) | 1 |
| **— SUITE STABILIZATION (consolidated-suite reliability — gates clean G4 runs) — ✅ COMPLETE: 10x-green consolidated gate PROVEN STABLE (1803/2/0 ×10) —** |
| STAB-ingestion | Eliminate cross-test global event-loop pollution that failed 112/112 ingestion tests in the consolidated suite (pytest-asyncio 1.4.0 restored `set_event_loop(None)` after `tests/control_plane`; ingestion's synchronous `get_event_loop().run_until_complete()` then raised) | `data-ingestion-engineer` | M1 / A | — | G3 | **DONE** (dual G3 PASS, att.1; **TEST-LAYER ONLY — zero production change.** Replaced all 94 `asyncio.get_event_loop().run_until_complete()` with self-contained `asyncio.run()` across 6 ingestion test files + defensive session-scoped autouse `tests/ingestion/conftest.py`; zero assertions/test-logic changed (382 real asserts intact). Source-confirmed: 1 residual `get_event_loop().run_until_complete` is a doc-comment only. Evidence: 5x+6x consolidated runs all **1083 passed/2 skipped/0 fail**, full suite **1802/2**, ingestion isolation **333**. backtest-qa: 5 source mutations (T-300a wall-clock leak / staleness / recorded-at honesty / backfill / pending event_date) all drive their guards RED, byte-restored — fix did NOT gut the no-leak guards. **STABILITY GATE → PASS: consolidated suite PROVEN STABLE — 1803 passed/2 skipped/0 failed, bit-for-bit identical across 10 consecutive deterministic runs (PYTHONHASHSEED=0, `-p no:cacheprovider`, `__pycache__` purged each run); the only non-pass = 2 allowed solders-gated execution skips (`test_tx_builder.py:161/:186`, verified `-rs`).** `05-reports/gates/G3-stabilization.md`) | 1 |
| **— WAVE P4: INTEGRATION (G4) —** |
| T-400 | Full sim/paper burn-in; purged/embargoed walk-forward; lookahead leak audit; clock audit (block_time vs arrival, C-5); group-aware purge (C-10) | `backtest-qa-engineer` | — | all G3 PASS | G4 | **DONE (FINDINGS)** | 1 |
| T-401 | Edge-vs-baseline gate on RECORDED data: GATE-A + GATE-B both, lower 95% bound > 0; calibrated haircut (C-11); experiment-log deflation (C-9); tip-contention + independent-surface stratification (C-3, C-13) | `backtest-qa-engineer` | — | T-400 | G4 | **DONE (UNPROVEN-NO-REAL-DATA — harness BUILT+CORRECT)** | 1 |
| T-402 | END-TO-END: running bot (PAPER) driven through dashboard AND Telegram — kill flattens within budget, mode propagates, feed shows real events; safety (breaker/survivable-stop/DMS) fire on demand in QA | `backtest-qa-engineer` | — | T-341, T-352, T-361, T-320, T-321, T-322 | G4 | **DONE (PASS)** | 1 |
| T-403 | Security + custody + LLM-prompt-injection audit; no real keys in code/logs/images; program-ID allowlist on signing; Telegram-command authz verified | `crypto-security-engineer` | M5 | T-251, all G3 PASS | G4 | **DONE (FINDINGS — core PASS, custody-impl conditional)** | 1 |
| **GATE G4 — Orchestrator: full QA green + security PASS + cross-component E2E.** | | | | T-400..T-403 | G4 | **PASS (conditional)** — record: `05-reports/gates/G4-PASS.md`. Leak/clock NON-VACUOUS; edge harness BUILT+CORRECT (`UNPROVEN-NO-REAL-DATA` = correct honest PAPER outcome); e2e PASS (kill<2s both surfaces, de-risk-only, 3 safety layers fire); security core PASS. **Carried (NON-blocking PAPER): COND-G4-1** non-hermetic `test_concurrent_thousand_snipes_one_winner` (→`agent-orchestration-engineer`, freeze injectable clock) · **T-402-F1** breaker not projected to StateStore (`snipe_loop.py:179`, →`agent-orchestration-engineer`/`solana-systems-architect`). **COND-G4-2 = HARD R3/LIVE checklist (NOT a PAPER blocker): F-01** signer refusals unbuilt + **F-10/F-07** image/host hardening + **F-02/F-03/F-04** supply-chain — must be built+test-proven before `DRY_RUN_ENABLED=false`. Real capital stays DRY-RUN-disabled. | — |
| **— WAVE P5: RELEASE (G5) — ✅ COMPLETE (G5 PASS, `G5-PASS.md`) —** |
| T-500 | ONE `docker compose up` (bot + control-plane + dashboard + Telegram + Redis + monitoring) + colocation/RPC/staked-connection plan | `latency-devops-engineer` | M5 | G4 | G5 | **DONE** (dual PASS — build self-check exit 0 + `code-reviewer` PASS, blocking=[]; one file → 11 services dependency-ordered; `docker compose config --quiet` exit 0 on Docker 29.2.0; DRY_RUN_ENABLED=true default on all 5 tx-capable services (RENDERED config); redis & `aats-signer` NO published ports (ADR-0009); startup self-check fail-closes exit 1 on `DRY_RUN_ENABLED=false` w/o `PRE_LIVE_CHECKLIST_SIGNED=yes`; colo plan honest+traceable to `latency-budget.md`; no secrets tracked. **Open R3-only (carried into COND-G4-2):** F-01 signer refusals scaffold + F-10 placeholder Dockerfile digests + F-07 host hardening — NOT PAPER blockers) | 1 |
| T-501 | Docs: README, deploy/ops guide, dashboard + Telegram operator guides, kill-switch runbook, staged-rollout guide (sim → shadow/record → paper/dry-run → tiny-real → scale) | `docs-delivery` | — | G4 | G5 | **DONE** (dual PASS att.2 — `code-reviewer` re-review PASS, blocking=[]. BLOCKER R-501-01 FIXED: false verified-output (`pytest tests/risk` doc'd as 337; inverted "in the 337 above" footnote) corrected by re-execution — tests/risk=315, tests/validation=22, combined=337; every printed command in README §6 + deploy-ops §10 now matches reality; repo-wide grep no remaining 337 claim. Honest framing intact: NO win-rate, edge `UNPROVEN-NO-REAL-DATA`, real capital DISABLED, R0-R4 ladder + pre-live checklist. No `aats/`/contracts touched. **Carry to T-600:** refresh README §6 `1803→1842` stale-prose suite count post-G4-fixes) | 2 |
| G4-fixes | COND-G4-1 (hermetic concurrent test — freeze injectable clock) + T-402-F1 (project breaker into StateStore) + QA-G4FIX-1/-2 (CPU-time latency test) | `agent-orchestration-engineer` | M3 / D | G4 | G5 | **DONE** (dual PASS att.2 — TWO independent `code-reviewer` re-reviews, both PASS, blocking=[]. COND-G4-1: `test_snipe_handoff.py:146` `frozen_clock=lambda:0` into `InMemoryStateStore(clock=...)` (production-supported seam `state.py:186/216/219/224`) → `0>=30000` False forever → lock-TTL-expiry impossible during 1000-thread storm; single-winner invariant mutation-proven RED (`state.py:234`/`claim_entering` mutated→RED, byte-restored). T-402-F1: `fast_loop.py:385` `save_breaker_state` projection (production change) — de-risk-only read path; covering test drives REAL `FastLoop.tick()`, mutation-proven (comment line 385 → RED, restored). QA-G4FIX-1: `test_t342_enforcer.py` CPU-time + module-logger suppression (log I/O off CPU-budget critical path; wall-clock AC-026 gate preserved by unchanged single-position test). Suite 1842/2/0 ×3 deterministic per EACH reviewer; 8x-concurrent COND-G4-1 repro held. NOTE: engineer handoff JSON under-declared changeset (claimed test-only; included `fast_loop.py:385` prod change) — caught+flagged by both reviewers, all inspected PASS) | 2 |
| **GATE G5 — Orchestrator: deploy verified + docs complete.** | | | | T-500, T-501, G4-fixes | G5 | **PASS** — record: `05-reports/gates/G5-PASS.md`. ONE `docker compose up` validated (11 services, config exit 0) + documented; docs/runbooks complete + honest (no win-rate, edge UNPROVEN-NO-REAL-DATA, real capital DISABLED, R0-R4 staged rollout + pre-live checklist); BOTH G4 carry-forwards FIXED + mutation-proven (COND-G4-1 hermetic, T-402-F1 projected); consolidated suite GREEN **1842 passed / 2 skipped / 0 failed** (the concurrent test now hermetic; 2 skips = allowed solders-gated `test_tx_builder.py:161/:186`) per recorded dual-reviewer execution. **Carried (NON-blocking PAPER): COND-G4-2 = HARD R3/LIVE checklist** (F-01 signer refusals + F-10 digests + F-07 host hardening + F-02/03/04 supply-chain) before `DRY_RUN_ENABLED=false`; Runtime to re-run the suite once to refresh the recorded count; T-600 to refresh README §6 1803→1842. | — |
| **— WAVE P6: ACCEPTANCE (G6) —** |
| T-600 | Delivery package: one command brings the system up; dashboard AND Telegram drive the bot live in PAPER; attach HONEST edge report (net-of-cost PnL + model-vs-baseline on recorded data) — NO win-rate marketing number | `docs-delivery` | — | G5 | G6 | **DONE** (dual G3 PASS — `code-reviewer` PASS, blocking=[]; `DELIVERY.md` + `HONEST-EDGE-REPORT.md` written client-ready + honest; README refreshed 1803→1842. Verified by execution: full suite **1842/2/0**, T-402 e2e **16 passed** (dashboard+Telegram drive bot in PAPER, kill flattens <2s, 3 safety layers fire, de-risk-only), `docker compose config` exit 0, venue DRY_RUN default. HARD RULES honored: NO win-rate number/target/claim anywhere, edge `UNPROVEN-NO-REAL-DATA`, real capital DISABLED behind DRY-RUN, money int/Decimal, no secrets. All 5 BRIEF §4 criteria MET) | 1 |
| **GATE G6 — Acceptance (agency-autonomous per `AUTONOMY-DIRECTIVE.md`).** | | | | T-600 | G6 | **ACCEPTED (agency-autonomous)** — record: `05-reports/gates/G6-ACCEPTED.md`. All 5 BRIEF §4 criteria MET (paper end-to-end vs SimulationVenue + net-of-cost PnL/model-vs-baseline · breaker+survivable-stop+DMS PROVEN-BY-FIRING · honeypot/rug rejected in sim · one `docker compose up` + `.env.example` + monitoring live · README+deploy/ops+kill-switch runbook). HARD RULES hold: NO win-rate, edge `UNPROVEN-NO-REAL-DATA`, real capital DISABLED behind DRY-RUN, suite GREEN 1842/2/0. **CORE BUILD G0–G6 COMPLETE.** Carried (NOT a G6/PAPER blocker): PRE-LIVE (R3) checklist A/B/C all NOT MET before `DRY_RUN_ENABLED=false`. | — |
| **— ENHANCEMENT PROGRAM (`ENHANCEMENT-DIRECTIVE-E1-E13.md` §ORDERING — runs LAST, AUDIT-FIRST, ADDITIVE) · WAVE 1 — record `05-reports/gates/ENH-wave1.md` —** |
| **E1** | Devnet live-send validation mode — `SOLANA_CLUSTER=devnet` actually submits→confirms→reconciles on worthless-SOL devnet behind the seam (mainnet LIVE unchanged + hard-gated) | `solana-execution-engineer` | M4 / C | G6 | G3 | **DONE — FIXED (dual G3 PASS, `code-reviewer` + `backtest-qa-engineer`, `05-reports/gates/ENH-wave3.md`)** (the re-fix LANDED + both reviewers independently re-ran. **BLOCKER-1 FIXED:** `reconcile()` now gates `landed=True` on `submitted AND signature AND reason in ("landed","devnet_landed")` (`jito_jupiter_venue.py:1004-1006`) — an unconfirmed devnet tx (`reason="devnet_confirm_failed:*"`, `land_slot=None`) reconciles `landed=False`; idempotency set keyed on `fill.landed` (`:342-343` execute, `:463-464` exit) → unconfirmed intents stay RETRYABLE. Mutation-proven: revert confirm-gate → `test_unconfirmed_devnet_tx_reconciles_as_not_landed` RED (1 failed/200). **BLOCKER-2 FIXED:** autouse `_env_isolation` fixture (`tests/execution/conftest.py:55-89`) snapshots+restores 9 venue env vars via monkeypatch; raw `os.environ` mutations replaced by `monkeypatch.delenv` (grep-clean) → DRY_RUN→LIVE leak HARD-CLOSED. **MAJOR-1 FIXED:** `_send_and_confirm_devnet()` reads `DEVNET_CONFIRM_MAX_POLLS`/`_POLL_INTERVAL_S` from env, passes as kwargs (`:755-765`). Determinism: `tests/execution` 201 passed/2 skipped across 6 PYTHONHASHSEED seeds + 8 consecutive + 5x hostile `DRY_RUN_ENABLED=false` — env-leak flake GONE. `SubmitMode.DEVNET` (ADR-0013) cannot unlock mainnet LIVE; DRY-RUN default; devnet=worthless SOL. mypy clean on the 2 changed prod files. Real capital stays DRY-RUN-disabled.) | 1 |
| **— ENHANCEMENT WAVE 3 (slow-loop adversarial signals — record `05-reports/gates/ENH-wave3.md`) —** |
| **E6** | Discord ingestion adapter — Bot-token-only, ALLOWLIST-gated, ToS-safe (no self-bot); coordinated shill LOWERS conviction; MCS slow-loop sentiment | `nlp-sentiment-engineer` (build) + `code-reviewer` + `backtest-qa-engineer` (dual G3) | M1 / A | G6 | G3 | **DONE — ADDED (dual G3 PASS, `05-reports/gates/ENH-wave3.md`)** (`source` Literal extended `"discord"` (`sentiment/models.py:45`); `DiscordAdapter`+`MockDiscordClient` (`sentiment/adapters.py`). De-risk-only: shill 0.191 vs organic 0.418, penalty 0.550, `coordinated_shill_flag=True`; `assert_mcs_cannot_size_up` identity for conviction {0,0.5,0.99,1.0}; MCS bounded [0,1]. Slow-loop only (NO FAST-path importer; 20 posts→1 LLM call). PIT: future excluded FIRST op, t+1ms byte-identical, event_time from message ts. Injection→quoted-untrusted, conviction 0.243≠1.0. `bot_token=None` offline→`[]`; no self-bot field; `.env.example` E6 placeholders; no `win_rate`. 93 sentiment tests. **MINOR (non-blocking):** F1 dead offline-seam docstring; **F2** comment cites `DISCORD_ALLOWLIST` vs env `DISCORD_CHANNEL_ALLOWLIST` (`adapters.py:363`); snowflake→age formula validate at deploy.) | 1 |
| **E7** | News / breaking-news layer — credible NEGATIVE event (TIER_1/2) → FORCE_EXIT/VETO_ENTRY; positive news contributes 0 (de-risk-only) | `nlp-sentiment-engineer` + `llm-reasoning-engineer` (build) + `code-reviewer` + `backtest-qa-engineer` (dual G3) | M1/M2 | G6 | G3 | **DONE — ADDED (dual G3 PASS, `05-reports/gates/ENH-wave3.md`)** (`sentiment/news_scorer.py` keyword heuristic ZERO LLM calls; `NewsSignal.mcs_delta` clamped **[-1.0, 0.0]** (`models.py:280,295`); `Reasoner.adjudicate_with_news()` (`reasoning/reasoner.py:193`). De-risk-only by construction: positive/neutral=0.0 (TIER1 -0.50/TIER2 -0.35/TIER3 -0.125); `credible_negative_event`→`narrative_failure=True`→FORCE_EXIT(open)/VETO_ENTRY(none) via `DeRiskIntentFactory`; `ReasoningAction`={HOLD,VETO_ENTRY,REDUCE_SIZE,FORCE_EXIT} — EntryIntent/size-up INEXPRESSIBLE BY TYPE; STRONG_BUY LLM + neg news → FORCE_EXIT. Slow-loop only (news ref'd only by sentiment+reasoning; FAST enforcer no import). PIT: shift-back removes effect, fetch_time≠event_time, future excluded belt+suspenders. Injection→`mcs_delta=0.0` quoted-untrusted. RSS NotImplementedError caught by `gather(return_exceptions=True)`→zero-news no crash. 192 sentiment+reasoning tests; zero secrets; no `win_rate`.) | 1 |
| **E9** | Alpha-caller / call-channel track-record scoring (HONEST) — selectivity weight [0,1], NO win-rate; most callers score near-zero/negative | `nlp-sentiment-engineer` (build) + `code-reviewer` + `backtest-qa-engineer` (dual G3) | M1 / A | G6 | G3 | **DONE — ADDED (dual G3 PASS, `05-reports/gates/ENH-wave3.md`)** (`sentiment/caller_score.py` 380L; ref'd only by itself+test, pipeline+Parquet store deferred to T-401). Selectivity-only: `mcs_delta_contribution<=0` always; weight [0,1] never raises conviction; `assert_caller_signal_cannot_raise_conviction` LIVE (forged +0.3→AssertionError); always-wrong/thin→weight 0. Slow-loop only (logging/math/dataclasses/typing only; offline-proven, blocked socket). PIT/leak-free: `outcome_event_time_ms<=decision_time_ms` BEFORE aggregation; 50 future wins→byte-identical score. NO `win_rate` (grep-clean) — metric is accuracy delta vs universe prior. **Mutation 3/3 KILLED:** positive-delta 10 RED / remove-PIT 3 RED / net-neg weight-1.0 6 RED, byte-restored. 257 tests (sentiment+models). **MINOR:** unused `field` import `:51`; re-validate `<=0` contribution when wired @G4.) | 1 |
| **E10** | Social-velocity + organic-vs-bot growth-ratio features — bot-driven velocity LOWERS conviction; ships DORMANT (un-wired) | `feature-quant-engineer`/`nlp-sentiment-engineer` (build) + `code-reviewer` + `backtest-qa-engineer` (dual G3) | M1 / A | G6 | G3 | **DONE — ADDED (dual G3 PASS, `05-reports/gates/ENH-wave3.md`)** (`sentiment/velocity.py` VelocitySignalComputer+Protocol+InMemoryVelocitySource+3 penalty components; ZERO importers — ships DORMANT). De-risk-only: `mcs_penalty>=0` always (subtractive); coordinated/bot/factory growth only RAISES penalty; 2000-trial fuzz never raises conviction; mutation-proven (neuter bot-velocity→1 RED, negate total→14 RED). Slow-loop only (no FAST coupling, no net/LLM/disk). PIT: filter FIRST op, future excluded+counted, 30 future events leave penalty pinned 1.0. Injection-safe (metadata only, no raw text). Zero secrets; no `win_rate`. 202 sentiment tests; ruff+mypy clean. **DEF-E10-01 (MAJOR, NON-BLOCKING for dormant unit, →T-313 wiring before G4):** de-risk guard is a bare `assert` — `python -O` strips it; forged negative penalty could raise conviction. NOT reachable via `compute()` (components clamped `>=0`) + un-wired → MAJOR not BLOCKER; **MUST clamp-without-assert BEFORE E10 feeds live MCS.** MINOR: F1 fabricated `AGE_FULL_PENALTY_DAYS` comment, F2 non-existent `text_hash` docstring field, F3 age validated in builder only.) | 1 |
| **E4** | Control-plane auth + exposure hardening — destructive POSTs operator-Bearer-gated (audited adequate); loopback-default bind + fixed Dockerfile entrypoint + nginx TLS/IP-allowlist recipe | `crypto-security-engineer` (build) + `code-reviewer` (G3) | M5 | G6 | G3 | **DONE — ADDED (dual G3 PASS, `code-reviewer` ×2, blocking=[])** (AUDIT: destructive-POST auth already adequate (COVERED); the real gap was the control plane could not start + would bind 0.0.0.0. **ADDED:** `app.py:54` `DEFAULT_BIND_HOST="127.0.0.1"` + `resolve_bind_host` (non-loopback honored but SCREAMS `SECURITY:`); `Dockerfile.controlplane:62` CMD fixed to `python -m aats.control_plane.app` (was broken nonexistent module + bare `--host 0.0.0.0`); `deploy/nginx/` TLS+deny-by-default-IP-allowlist+rate-limit recipe; `controlplane-bind.override.yml` re-publishes `127.0.0.1:8787:8787` (base `docker-compose.yml` UNTOUCHED); `.env.example` E4 placeholders only. Mutation-meaningful: auth-check→no-op → 8 rejection tests RED, restored byte-identical. `tests/control_plane -q` 126 passed (incl. 14 new `test_bind_exposure.py`). Telegram authz operator-only + `pause()` hard-codes SHADOW (cannot move mode up). **Non-blocking follow-ups routed:** MINOR `server.py:76` `operator_token` defaults to literal `"dev-token"` when unset → fail-closed (→`backend`/`crypto-sec`); nginx placeholders → `latency-devops`; F-01/F-03 pre-existing R3 preconditions.) | 1 |
| **E5** | Always-on operational hardening — systemd units (compose stack + control plane + Redis backup timer) + logrotate + Redis backup/restore | `latency-devops-engineer` (build) + `code-reviewer` (G3) | M5 | G6 | G3 | **DONE — ADDED (G3 PASS, `code-reviewer`; ops/deploy — no model/trade-path logic so no backtest-qa lane)** (AUDIT: `startup-self-check.sh` already adequate (COVERED). **ADDED:** `deploy/systemd/aats.service` (ExecStartPre runs startup self-check; **never sets `DRY_RUN_ENABLED=false`** `:66-69`; wraps not replaces compose) + `aats-controlplane.service` + `aats-backup.service`/`.timer` (daily 03:00 UTC, Persistent); `deploy/logrotate/aats` (decisions 90d/startup 8w/backup 12w/docker 7d); `scripts/redis-backup.sh` (`set -euo pipefail`, BGSAVE+LASTSAVE poll, gzip -9 + `gzip -t` integrity, retention prune, `DRY_RUN_BACKUP=true` smoke mode, NEVER consults `DRY_RUN_ENABLED` — read-only from Redis); `docs/redis-backup-restore.md`. Verified: `bash -n` OK ×2, dry-run exit 0 writes nothing, startup self-check 10/10, systemd lint 4/4, logrotate brace 4/4, pure-LF, secret scan 0 hits, `docker-compose.yml` UNCHANGED. **Limitation (documented, not blocking):** `systemd-analyze verify`/`logrotate --debug` need a Linux host — byte/structural validation substituted; decision-log stanza needs a host volume mount (operator step). `G5-E5-EVIDENCE.md`.) | 1 |
| **— ENHANCEMENT WAVE 4 (FINAL — read-only operator visibility surfaces + the E10 guard fix — record `05-reports/gates/ENH-COMPLETE.md`) —** |
| **E3** | Candidate-queue surface — `GET /api/candidates` read-only API + dashboard page: every launch the bot evaluated and why it skipped/sniped | `backend-engineer`/`agent-orchestration-engineer` (API) + `frontend-engineer` (page) + `code-reviewer` + `backtest-qa-engineer` (dual G3 API) | M3/E | G6 | G3 | **DONE — ADDED (dual G3 PASS API + `code-reviewer` PASS→backtest-qa PASS page, `05-reports/gates/ENH-COMPLETE.md`)** (additive read-only endpoint. `CandidateQueue` bounded ring `maxlen=200` thread-safe (`controller/candidate_store.py`); `CandidateRecord`+`SafetyReport` Pydantic schemas (`control_plane/candidate_schemas.py`, NOT in frozen `aats/contracts/`); `SnipeLoop._record_candidate()` records at every decision exit (no-op when queue None — backward-compatible); injectable `candidates_provider` (`control_plane/server.py`). §12 frozen endpoint list UNCHANGED; §14 additive delta note in `api-contracts.md`. 16 API tests; 142 control_plane + 111 controller. Both reviewers enumerated routes (GET-only, true 405 on POST), drove `process_event` through every decision branch (reason matches in EVERY branch), confirmed NO `win_rate`, NO money/lamport field (`model_p` is a probability float). **Page:** BLOCKER E3-B1 fixed (adapter re-typed to authoritative `CandidateRecord`, safety derived from `safety_report` alone, fail-closed on missing `gate_passed`); read-only GET-only no control; 89/89 dashboard tests, mock build GREEN. **MINOR (non-blocking):** nested-dict win_rate/money guard is top-level only (unreachable — no producer wired); `CandidateQueue.snapshot()` returns same dict objects vs docstring.) | 1 |
| **E11** | Wallet-cluster "Bubble Maps" — `GET /api/wallet-cluster` read-only API + SVG graph page showing coordinated bundler/sniper clusters (projection of EXISTING detection) | `backend-engineer`/`agent-orchestration-engineer` (API) + `frontend-engineer` (page) + `code-reviewer` + `backtest-qa-engineer` (dual G3 API) | M3/E | G6 | G3 | **DONE — ADDED (dual G3 PASS API + `code-reviewer` PASS→backtest-qa PASS page, `05-reports/gates/ENH-COMPLETE.md`)** (typed `WalletClusterGraph`/`WalletNode`/`WalletEdge` Pydantic schema (`control_plane/wallet_cluster_schemas.py`); `WalletClusterStore` bounded ring parallel to CandidateQueue (`controller/wallet_cluster_store.py`); additive GET-only endpoint w/ `wallet_cluster_provider`, `[]` when unwired (`control_plane/server.py`). **NO new detection logic** — projects EXISTING data: `sniper_cluster_score`/`bundling_detected`/`bundler_wallet_count` (`features/microstructure.py`), `dev_bundle_cluster_bps` (`risk/pretrade_gate.py`). Read-only is FRAMEWORK-enforced (GET-only route, true 405). All shares int bps [0,10000] w/ validators; scores/weights bounded statistical floats; NO money, NO `win_rate`, fail-safe `[]`. 15 API tests; 157 control_plane (deterministic ×2). **Page:** SVG-native (no recharts) code-split "Bubble Maps"; typed adapter w/ dangling-edge drop + bps/score clamps; GET-only hook `[]`-fallback; 107 dashboard tests (+18). **MINOR (QA-E11-001, non-blocking, shared w/ E3):** nested node/edge dicts bypass top-level win_rate/money guard — unreachable in prod (no producer wired, endpoint returns `[]`); future Pydantic-coercion-at-response hardening covering E3+E11.) | 1 |
| **E7-page** | Narrative & News dashboard page (deferred E7 UI half) — per-asset narrative cards, de-risk `mcs_delta`∈[-1,0], coordinated-shill/breaking red flags | `frontend-engineer` (build) + `code-reviewer` (G3) | E/M1 | G6 | G3 | **DONE — ADDED (`code-reviewer` PASS→backtest-qa PASS, `05-reports/gates/ENH-COMPLETE.md`)** (read-only Narrative page: per-asset cards render sources (outlet+credibility tier), freshness, de-risk score (`mcs_delta`∈[-1,0], never a win-rate), coordinated-shill/breaking flags, headline digest; loading/empty/error/populated states. Typed client `useNarrative`+`adaptNarrative`+`WireNarrativeSignal` mirrors backend `NewsSignal` (`sentiment/news_scorer.py`+`models.py`), GET-only w/ mock fallback. **De-risk invariant ENFORCED IN THE ADAPTER:** `clampDeRiskDelta` forces [-1,0] — a positive/hostile frame clamps to 0, NEVER a buy signal; NaN→0. Read-only (no button/onClick/POST). 123 dashboard tests (21 narrative-specific); `mcs_delta`-clamp + tier-fallback + verbatim-passthrough mutation-guarded; tsc/eslint/build clean, GREEN on mock. NO win-rate (honesty-clause denials only), NO money-as-float. `aats/contracts`+`docker-compose.yml` untouched. **NIT (codebase-wide):** mock-key churn shared w/ E3/E11 mock factories.) | 1 |
| **DEF-E10-01** | E10 de-risk-guard fix — replace the bare `assert` (stripped under `python -O`) in `apply_velocity_penalty_to_conviction` with raise+clamp so the de-risk invariant holds under optimized mode | `llm-reasoning-engineer`/`nlp-sentiment-engineer` (build) + `code-reviewer` + `backtest-qa-engineer` (dual G3) | M1/M2 | G6 | G3 | **DONE — ADDED (dual G3 PASS, `05-reports/gates/ENH-COMPLETE.md`)** (the Wave-3 carry-forward MAJOR. In `sentiment/velocity.py` `apply_velocity_penalty_to_conviction`: replaced the bare `assert adjusted <= base_conviction + 1e-9` with (1) explicit `raise ValueError` on negative raw `mcs_penalty` (survives `python -O`), (2) `max(0.0, penalty_raw)` belt-and-suspenders clamp, (3) `raise ValueError` output guard. Proven under `python -O` (`__debug__=False`): forged `mcs_penalty=-0.5` RAISES (was silently raising conviction); legit bot-burst still lowers 0.7→0.0 no exception. `TestDeRiskGuardSurvivesOptimizedMode` (7 tests): exception is ValueError not AssertionError, fires under simulated `-O`, zero bare asserts via `inspect.getsource`, clamp independently load-bearing. **Mutation-proven:** revert to bare assert → 5/7 RED; mutated code under `-O` leaks conviction 0.7→1.0 (the exact asymmetric-trust violation closed). 209 sentiment tests; mypy clean; no secrets; SLOW-LOOP ONLY preserved. **MINOR:** docstring `[0.0, base_conviction]` wording vestigial.) | 1 |
| **GATE — ENHANCEMENT PROGRAM E1–E13 CLOSE-OUT** (record `05-reports/gates/ENH-COMPLETE.md`) | | | | E1..E13 + 4 audits + E3/E7/E11/DEF-E10-01 | ENH | **✅ COMPLETE — 16 ADDED · 1 COVERED (E8) · 0 FAILED.** 13/13 enhancements + 4/4 audits cleared; every dual-G3-gated item PASS; no regression to the G6-accepted core. Consolidated suite GREEN (Python 2283/2; dashboard build GREEN + 123 tests). Security re-audit **PASS** (`05-reports/security/ENH-security-reaudit.md` — no new CRITICAL/HIGH). HARD RULES hold program-wide (de-risk/visibility-only, safety primitives untouched, no win-rate, no secrets, money int/Decimal, `aats/contracts`+`docker-compose.yml` not edited). **THE WHOLE ENGAGEMENT IS DONE.** Real capital stays DRY-RUN-disabled + unreachable; the **R3 pre-live checklist** (Block A/B/C) remains the gate before `DRY_RUN_ENABLED=false` and is NOT cleared by this program. | — |

---

## 2. Competitive-feature → lane traceability (BUILD-DIRECTIVE-v3 §"Competitive feature target")

Every named competitor feature is owned by a task above and surfaced in an operator surface.

| Competitive feature | Owning task(s) | Lane / Module | Operator surface |
|---|---|---|---|
| Auto-sniper on new launch | T-300, T-340 | A/M1 + D/M3 | dashboard `/feed`, `/` |
| Migration sniper (pump.fun → PumpSwap/Raydium) | T-300 (decoders), T-340 (loop), EH-003 selection | A/M1 + D/M3 | dashboard `/feed` |
| Copy-trade / smart-money SELECTIVITY (filter+trigger, never blind mirror) | T-302 (stream), T-304 (`smart_wallets_in` feature), T-324 (filter wiring) | A/M1 + C/M4 | new dashboard copy-trade page (T-353) |
| Limit + DCA resting orders (fire when offline) | T-326 | C/M4 | dashboard (T-353) |
| Auto TP-ladder + trailing + auto-strat presets | T-325 | C/M4 | Settings presets (T-353), `/positions` |
| Multi-wallet / bundle + partial fills + anti-cluster | T-328 | C/M4 | `/positions`, monitoring |
| MEV protection (fast vs secure / private) | T-331 | C/M4 | `/latency`, Settings |
| Token-safety scanner (honeypot, mint/freeze auth, LP lock, dev/bundle, holder concentration) | T-323 (gate), T-329 (sell-sim) | C/M4 | red flags on `/feed`, `/risk` (T-353) |
| Token discovery enrichment (DEXScreener/Birdeye/Meteora/Moonshot) | T-301 | A/M1 | `/feed`, `/` |
| Portfolio + P&L cards / export | T-353 | E | dashboard `/positions`, P&L cards |
| Telegram operator channel (alerts + constrained de-risk commands) | T-360, T-361 | F | Telegram |

---

## 3. Critical-path & parallelism notes

- **Serial spine:** T-000 (done) → P1 spec → **G0 (CEO)** → P2 arch → **G1 (CEO)** → P2.5 scaffold/custody → P3 lanes → P4 integration → **G4** → P5 → **G5** → **G6 (CEO)**. NO CODE before G1.
- **WAVE M1+C1 CLEARED (2026-06-16):** Lane A T-300/T-304/T-305 + Lane C T-323/T-324/T-325/T-327 + T-199 closure + T-351 — all dual G3 PASS (`G3-waveM1C1.md`). Lane D (T-340) still last — integrates A/B/C against SimulationVenue.
- **WAVE M2 CLEARED (2026-06-16):** Lane B T-310/T-311/T-312/T-313 + Lane A T-306/T-300a + Lane C T-329/T-330/T-331 — all dual G3 PASS (`G3-waveM2.md`), source-confirmed. **NOW UNBLOCKED → Wave M3 = Lane D controller:** T-340 (deps T-310✓/T-313✓/T-324✓/T-327✓) → T-341 (deps T-201✓/T-340) → T-342 (deps T-321✓/T-322✓/T-340). **Remaining Lane C enhancements:** T-326 (deps T-321/T-325 ✓), T-328 (deps T-327/T-251 ✓). **Remaining Lane A enrichment:** T-301/T-302/T-303 (deps T-300 ✓; T-301 also absorbs the T-300a pending-table carry-forward). Then **Lane E wire** T-352 (deps T-341/T-351 ✓) → T-353; **Lane F** T-360 (deps T-341) → T-361. Then **G4** (T-400/T-401 edge-on-recorded-data is where live edge is finally proven; survivor-MCS re-validation lands there).
- **Parallel after G1:** P2.5 T-250 ∥ T-251.
- **Parallel P3 lanes:** A, B, C, E, F can run concurrently once their deps clear; **Lane D (T-340) integrates A/B/C and is built last** against SimulationVenue. Lane E (T-350/T-351) can start on the scaffold before D's API exists, then T-352 wires once T-341 is up.
- **Worktree isolation:** Lane E (`./dashboard`) and Lane F (Telegram service) touch separate trees from the Python/Rust core — safe to parallelize. Within Lane C, the survivable-stop seam (T-321) is co-owned (risk + execution + orchestration) — sequence its sub-parts, do not run them on the same files simultaneously.
- **SAFETY-FIRST enforcement:** T-320 → T-321 → T-322 are the first Lane-C tasks and MUST be PROVEN (QA fires them on demand, T-402) before T-327's live-capable path is enabled. Real submit stays behind the DRY-RUN flag through P6.
- **G1 red-team delta absorbed into scope (BLUEPRINT §14; ADR-0009/0010):**
  - **ADR-0009 (custody topology reversal):** `sign()` crosses to a separate `aats-signer` process. Affected: **T-327** (venue `sign()` → loopback socket to signer, handle `SignerRefused`); **T-251** (implements signer + 3 refusals + Vault/`mlock`/zeroize); **T-352a** (NEW signer service); **T-250 / T-500** (signer compose unit + Vault wiring); **T-340 / T-342** (DMS pre-signed flattens produced via `aats-signer`). The control-plane wire contract is UNCHANGED.
  - **ADR-0010 (label/provenance guards):** **T-304 / T-305** each feature emits a `FeatureSourceWindow` provenance manifest; **T-400 / T-401** own the `LaunchOutcome` `labels/` writer + the four §2.5 build/load guards + the label-horizon/per-feature-lineage placebos; **T-310 / T-311** join labels by `event_time` from `labels/` only.

## 4. Conditions C-1..C-13 → task ownership (EDGE-VERDICT.md §"Conditions for GO")

| Condition | Owning task(s) |
|---|---|
| C-1 latency honesty | T-205, T-102 |
| C-2 no inherited optimism (cost stack) | T-204, T-401 |
| C-3 tip-cohort-bias kill / stratify | T-331, T-401 |
| C-4 freeze + build baseline | T-203, T-305, T-311 |
| C-5 clock audit + frozen haircut | T-400, T-401 |
| C-6 completeness audit | T-303 |
| C-7 clean-room harness + import guard | T-204, T-400 |
| C-8 R2 necessary-not-sufficient | T-105, T-401 |
| C-9 experiment log + deflation | T-401 |
| C-10 group-purge | T-400 |
| C-11 calibrated-haircut sub-gate | T-401 |
| C-12 regime + staleness | T-105, T-401 |
| C-13 independent-surface reporting | T-401 |

## 5. Three-strikes ledger

| Task | Attempts | Failures | Status | Action |
|---|---|---|---|---|
| T-250 | 2 | 1 (G3: honesty-clause win-rate KPI + CI baseline unrunnable) | **DONE** | Both blockers fixed + re-verified by 2 code-reviewers; no escalation. |
| T-320 | 2 | 1 (G3: BLOCKER B1 — restart restored-net seeded a fresh UTC day, could MASK a hard halt) | **DONE** | B1 fixed (event-time-day-key seeding + ADR-0012 + contract invariant); dual G3 PASS, proven-by-firing both B1 directions. No escalation. |
| T-322 | 2 | 1 (G3: BLOCKER R-01 I001 import sort + R-02 UP038, under CI-pinned ruff 0.4.10) | **DONE** | Both lint BLOCKERs fixed + re-verified; dual G3 PASS, mutation-meaningful. No escalation. |
| T-199 | 2 | 2 (G3 att.1: dead validator + false-coverage test; G3 att.2: intermittent `extra="forbid"` leak-guard non-enforcement BLOCKER + LatencyHop `cls`/`class` wire-key MAJOR) | **CLOSED** | Re-planned at attempt 2 (split T-199a leak-guard de-flake + T-199b LatencyHop alias). **Both DONE — dual G3 PASS on the in-tree diff** (`G3-waveM1C1.md`): leak guard ENFORCED + repeatable green across 14 PYTHONHASHSEED + mutation-proven; LatencyHop emits wire key `"class"`. The prior dispatch death was a PROCESS event, NOT a 3rd content strike. No escalation. |
| T-311 | 2 | 1 (G3 att.1: F-1 hash-inconsistent committed config BLOCKER — stored frozen_hash ≠ live canonical hash + vacuous git-clean proof on an untracked file; F-2 self-check didn't reflect delivered bytes MAJOR) | **DONE** | Re-stamped to a hash-consistent freeze (selection_percentile=60.0, frozen_hash=e1e7dc6c re-derived from params by construction), staged the deliverables so tested==tracked. Dual G3 PASS att.2 (`G3-waveM2.md`); C-4 control mutation-meaningful (post-fit param change → RED). No escalation. |
| T-340 | 3 | 1 (G3 att.2: BLK-2 mutation-vacuous mark=0 masks no-op'd `emergency_flatten_all`) | **DONE** | Att.3 test-only re-spin (+ R-1 prod fix `entry*0.60`). BLK-2 mark=7 isolates `emergency_flatten:`; FIX-2 `ExitConfig hard_stop_r=0.10` isolates `stop_exit:`; R-1 PINNED — all 3 mutation-proven RED by both reviewers. Dual G3 PASS. 1 content strike total; no escalation. |
| T-341 | 3 | 1 (G3 att.2: QA-MAJOR-1 widen-rejection tests mutation-vacuous; trap masked by 400 floor) | **DONE** | Att.3 test-only re-spin. Lex≠numeric trap `"10.0"<"2.5"` lex vs `10.0>2.5` on `daily_loss_limit_pct`+`jito_tip_cap_frac`; `_coerce`-neuter→4 RED. R-3 POST status codes added. Prod byte-unchanged. Dual G3 PASS. 1 content strike total; no escalation. |
| T-326 | 3 | 1 (G3 att.2: code-reviewer BLOCKER B2 + backtest-qa BLOCKER — 3 new B1 aggregate-cap regression tests FLAKE ~1/26-1/50 under full `tests/risk`, 49/50 on reproduction, reproducing the exact 0.75-vs-0.5-SOL breach they guard; root cause undiagnosed at att.2) | **NEEDS-REPLAN — att.3 LANDED + FLAKE-RESOLVED, dual-G3 VERDICT MISSING (NOT a content strike — no att.3 verdict)** | Att.3 fix IS in the tree and is the CORRECT diagnosis the att.2 backtest-qa BLOCKER demanded: the process-global decimal context (not a boundary constant) fed the within-tick aggregate clamp → **production fix** `sizing.py:89` `_hermetic_decimal_ctx()` + `:288` pinned prec=28/ROUND_HALF_EVEN inside `FractionalKellySizer.size`; new `test_sizing_hermetic_under_hostile_decimal_context` proves cap-holds + byte-identical across 63 hostile contexts. **The 10x consolidated stability gate has NOW RUN and is GREEN (1803/2/0 ×10, identical), exercising `tests/risk` incl. this covering test (+1: 1803 vs 1802) — the att.2 ~1/50 flake did NOT reproduce, so the flake is EMPIRICALLY ELIMINATED.** BUT the att.3 re-dispatch to obtain the dual-G3 verdict DIED (`status:FAILED, reason:"engineer died"`) — no `code-reviewer`+`backtest-qa` PASS on this SAFETY-path prod diff. A dispatch DEATH is a PROCESS event, NOT the 3rd content strike. **Re-entry (VERDICT-ONLY — fix landed, flake proven gone): re-dispatch dual G3 on the already-landed `aats/risk/sizing.py` + `tests/risk/test_resting_orders.py` diff; backtest-qa may cite this 10x gate as the stability proof.** If att.3 then fails dual G3 → that IS the 3rd content strike → CEO escalation (charter §3.5). OFF milestone path — runs ∥ G4. |
| STAB-ingestion | 1 | 0 | **DONE** | Dual G3 PASS att.1, test-layer only (zero production change), source-confirmed; consolidated suite reliably green (1083/2/0 ×6 runs, full suite 1802/2). No-leak guards proven still-RED-under-mutation. **Stability gate NOW RUN — PASS: consolidated suite PROVEN STABLE, 1803/2/0 ×10 identical (only non-pass = 2 allowed solders skips).** No escalation. `G3-stabilization.md`. |
| T-400 | 1 | 0 | **DONE (FINDINGS)** | G4 leak/clock/burn-in. Leak/clock guards proven NON-VACUOUS (all RAISE on planted leaks re-run this session); 214 leak/clock/gate tests PASS; join event-time-only; no `truth_*`/no `sniper_sim`. Verdict FINDINGS not PASS because (a) one non-hermetic concurrent test (COND-G4-1, NOT a leak/harness/safety fail) and (b) T-401-scope harness gaps reported-not-credited. G4-acceptable. No escalation. `G4-PASS.md`. |
| T-401 | 1 | 0 | **DONE (UNPROVEN-NO-REAL-DATA)** | G4 edge proof. Harness BUILT+CORRECT (source-verified `aats/models/gate_a.py` + `tests/validation/*`): right sign on model-wins/model-loses controls, declines→0, net-of-cost, deterministic, clean-room guard non-vacuous, purge load-bearing. `UNPROVEN-NO-REAL-DATA` is the CORRECT honest PAPER outcome (no recorded mainnet data; all corpora `is_bootstrap_not_real`) — G4 does NOT require proven edge. No escalation. `T-401-edge-proof.md`. |
| T-402 | 1 | 0 | **DONE (PASS)** | G4 e2e PAPER operator demo. 16 tests green: KILL flattens <2s from dashboard AND Telegram (non-vacuity proven); MODE propagates; SSE feed real `provenance:live_controller` frame; breaker + L2 stop + L3 DMS fire on demand; risk-increase/no-auth rejected; de-risk-only. Findings F1 (breaker projection MAJOR, source-confirmed `snipe_loop.py:179`) + F2 (=COND-G4-1) + F3 (latency flake) filed for owners. No escalation. `T-402-e2e-demo.md`. |
| T-403 | 1 | 0 | **DONE (FINDINGS — core PASS)** | G4 security/custody/prompt-injection. Secrets PASS, Telegram authz PASS, DRY-RUN PASS, prompt-injection PASS (size-up type-inexpressible); 452 tests pass. Findings: F-01 (HIGH-latent: signer refusals unbuilt scaffold), F-10 (HIGH-for-LIVE: placeholder image digests), F-02/F-03/F-04 (MEDIUM/LOW supply-chain) = COND-G4-2, the HARD R3/LIVE checklist (NOT a PAPER blocker; LIVE hard-gated off + unfunded wallet make F-01 latent). No escalation. `G4-security-audit.md`. |

_On the 3rd failed attempt of any task, the Orchestrator re-plans (split / change approach / reassign); if re-planning fails, escalate to CEO with 2–3 options + recommendation (charter §3.5)._
