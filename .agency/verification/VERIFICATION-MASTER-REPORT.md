# AATS — VERIFICATION MASTER REPORT (M3 · 5-lane dual-G3)

> Read-only verification campaign. 30 agents: 5 lane auditors → per-lane dual-G3 (`code-reviewer` + `backtest-qa-engineer`) → adversarial REFUTE on RED findings → completeness critic. All Opus 4.8. **Real capital is DISABLED; the edge is a genuine NO-GO on launch data — that is the edge proof working, not a bug.**

**Date:** 2026-07-07 · **Run:** wf_81b765a7-7f8 · **Findings:** {'RED': 2, 'YELLOW': 32, 'GREEN': 33} · **RED survived adversarial verify:** 2

## Lane status
| Lane | Status | Rationale (summary) |
|---|---|---|
| A-blockchain-core (ingestion / execution / mev / backtest) | **RED** | The one control ADR-0009 designates as the sole bound on unbounded loss — the aats-signer per-tx lamport cap + program/tip allowlist enforcer — does not exist: rust/aats-signer is a self-labeled SCAFFOLD serving only GET /health, never binds SIGNER_SOCKET_PATH, loads no key, enforces none of the three refusals. The Python venue defaults BOTH signer_client AND rpc_client to mocks (MockSignerClient at jito_jupiter_venue.py:180, MockRpcClient at :179), so a misconfigured LIVE deploy that forgets to inject real clients would 'land' phantom fills with zero network calls. Compounding capital-correct… |
| B-trading-safety | **YELLOW** | The safety spine that matters most is verified sound (GREEN): the circuit breaker keys the day off on-chain block_time, rejects float money by type, is asymmetric-trust by construction (trip_from_llm can only de-risk; reset needs a genuine OperatorResetToken no automated/LLM path can mint), re-reads TRIPPED on restart and never auto-resets, and is live-wired in __main__. The leak boundary (assert_features_leq_decision raises LeakError, decision/outcome functions never share an argument) and the conservative realizable_exit invariant (realizable ≤ spot by construction + clamp, hash-frozen) are … |
| C-platform-infra | **YELLOW** | Capital is genuinely fenced (DRY_RUN default true; ceo_token='' makes LIVE fail-closed; the dead-man's switch is a verified true separate failure domain — fail-closed on missing/unreachable heartbeat, NTP-step resistant via max(wall,monotonic) age, latch-before-submit, non-disarmable without a real DmsStandDownToken; decision-log stamps event-time not wall-clock). But the operator/observability control plane has a cluster of YELLOWs that MUST be promoted to RED before any host exposure or LIVE: (1) the destructive control plane publishes on 0.0.0.0:8787 with operator auth defaulting to the pub… |
| D-frontend-governance | **RED** | In the default docker LIVE build (VITE_USE_MOCK=false, browser at :3000 → :8787 cross-origin) the operator control surface controls nothing against the real backend: postJSON sends no Authorization: Bearer operator token, the dashboard nginx is static-only with no /api proxy to inject one, and there is NO CORS anywhere in aats/. Every operator POST (kill/flatten/flatten-mint/breaker-reset/mode) 401s and is CORS-preflight-blocked; reads and the /api/feed EventSource are CORS-blocked too. Mirror-image defect in the DEFAULT build (USE_MOCK=true): killSwitch/flattenAll/resetBreaker/setMode are loc… |
| E-security-edge | **YELLOW** | No present-exploitable breach and the headline edge verdict is honest: the launch/momentum NO-GO on n=4,187 is genuine (a small-sample n=497 GATE-B PASS correctly reversed by 8x more data; the realizable exit only makes outcomes more conservative so it cannot manufacture a GO; the GO path is reachable in tests so the NO-GO isn't rigged). Key isolation is GREEN by design (no private key in code/env/logs; hot core holds pubkey only; secret is a Vault path; signer forces WARNING and never logs tx bytes), and the edge-proof harnesses are verified leak-free and fail-closed (37/37 + 81 tests pass; R… |

## Top cross-cutting risks
1. EDGE-HARNESS INTEGRITY (spans A/B/E — the primary capital-licensing risk): run_edge_proof computes GATE-A/GATE-B in-sample via an iid trade-resample bootstrap over the entire real outcome list — no temporal train/test split, no purge of overlapping label horizons (momentum/reaction hold to 1800s so adjacent launches' horizons overlap), no embargo. The purged_embargoed_windows engine exists but is wired ONLY to the synthetic IS_BOOTSTRAP_NOT_REAL corpus. Real meme outcomes are correlated (shared regime), so the lower-95 bound understates variance and is optimistic in the exact capital-unlocking direction. Compounds with momentum's ~64s nominal-stamp timing optimism and the uncalibrated 150bps adverse-selection floor — all three bias GATE toward PASS. No real-data GATE-B PASS clears the charter's OOS bar until this is fixed; catastrophic in-sample leak becomes structurally possible the moment a trained ONNX classifier replaces the thin rule at THIN_MODEL_SEAM.

2. CUSTODY ENFORCER ABSENT (ADR-0009): the per-tx/rolling lamport cap + program/tip allowlist that is the sole bound on unbounded loss is a health-check-only scaffold, and the production venue defaults to BOTH MockSignerClient and MockRpcClient — a misconfigured LIVE deploy (live_submit_enabled=true + DRY_RUN=false) that forgets to inject real clients would clear the live gate and 'land' phantom fills with zero network calls. Fix must implement the signer with refusal-proving tests (over-cap tx, off-allowlist program) AND remove both mock defaults for the production venue before any key exists.

3. CONFIG-STRING PATHS TO REAL MONEY: DEVNET submit_mode bypasses all three DRY_RUN gates by design — pointing RPC_DEVNET at a mainnet URL routes a real send with no DRY_RUN check and no CEO auth (only barrier is the injected client being a devnet client; no genesis-hash assertion). The control plane binds 0.0.0.0:8787 with a public dev-token default. Real-money and destructive-control paths are gated by env strings, not defense-in-depth.

4. ILLUSORY OPERATOR + OBSERVABILITY SURFACE: the dashboard KILL/FLATTEN buttons 401/CORS-fail against the real backend and fake-success in mock; Alertmanager drains ALL P1 routes (breaker-trip, DMS-stale, heartbeat-dead) to an empty null receiver (doubly silent — no envsubst entrypoint either); Prometheus exposes unauthenticated lifecycle /-/reload and /-/quit on 0.0.0.0:9090. An operator would see green panels and success toasts while every page is dropped and every button is inert. The autonomous spine + Telegram are the only trustworthy controls today.

5. FALSE-ASSURANCE ENV-NAME SPLIT: compose injects CEO_AUTH_TOKEN / OPERATOR_API_TOKEN but the code reads CEO_TOKEN / OPERATOR_TOKEN, and compose does not forward unlisted host env — so an operator who sets a strong token has it silently dropped, the container keeps honoring the public dev-token, and the documented LIVE-enable path can never function. Worse than a missing token: it is silent false assurance on a safety-critical destructive surface.

6. MAINNET LIVE EXECUTION CORRECTNESS UNPROVEN: send_transaction reports 'landed' with no getSignatureStatuses poll on BOTH entry and exit, so a submitted-but-dropped tx becomes a phantom filled position; the idempotency dedup set is in-memory only (lost on restart → double-land risk); the Jito atomic-buy bundle is orphaned (tip burned via plain RPC, zero atomicity/MEV benefit); swap discriminator, System-transfer tip encoding, and ALT are placeholders. A cluster of capital-correctness bugs that all activate together the instant LIVE is enabled; a confirmation-poll fix must re-check the ORIGINAL signature before any resend to avoid double-spend.

7. INCOMPLETE ASYMMETRIC-TRUST GUARD: the /api/risk-config 'tighten-only' allowlist omits 6 of 14 fields — t_dms_seconds (DMS deadline), daily_loss_floor_lamports (breaker floor), veto_threshold (LLM veto sensitivity), blast_radius_cap, n_wallets_max, daily_risk_tranche can all be WIDENED with a 200 and persisted. Latent because the box is display-only today, but it contradicts the endpoint's advertised guarantee, is untested, and becomes load-bearing the moment anyone wires it to the live gate.

8. UNCERTIFIED CODE MERGED DURING AUDIT: reaction_harness.py (40KB, untracked) is wired into the runnable edge proof with ZERO tests and a docstring falsely claiming 'both tested RED-before/GREEN-after'; its gate CIs are iid bootstraps that are acute for the temporally-clustered, same-source-coupled reaction signals. The working tree changed between audit reads. Any G3/G4 sign-off must be pinned to a frozen commit and must not certify an untested leak guard.

## Missing coverage (NOT verified this pass — carry to M4)
- LLM asymmetric-trust at RUNTIME (reasoning LLM can only veto/force-exit, never size up / widen a stop / add leverage / override a hard stop) was verified by code SHAPE only; not exercised end-to-end in these audits (explicit out-of-lane note, must be validated before G4).
- Survivable-stop fires under process-kill — never exercised in any lane; claimed but unproven.
- SNIPE / FAST-loop latency SLAs — never exercised; no timing evidence.
- Full git-history secret sweep (gitleaks/trufflehog over history, not just the tracked tree spot-grep) — not run; the durable CI gate is only recommended.
- DryRunBlockEngineSubmitter no-transmit gating and dms_main.py env fail-close-to-60s parsing — explicitly NOT independently re-verified this pass (trust-but-note).
- Signer container unrestricted egress (audit F4/C5) and tx_builder placeholder discriminators (F6/C8) — go-live conditions omitted from this adjudication set; not paper-exploitable but unverified for LIVE.
- reaction_harness.py leak/reputation-leak/model-subset/frozen-drift guards — leak-safe on code-read but have ZERO covering tests, so uncertified.
- Real-corpus purged/embargoed walk-forward — never run; the OOS bar is unmet on real data (only the synthetic corpus exercises the walk-forward engine).
- AGGREGATE SAFETY POSTURE: enforcer_wiring, gate, reasoner, regime_wiring, and lp_unlock_schedule_source are ALL None simultaneously in __main__.py, so survivable-stop Layers 1-3, the honeypot/rug + deployer/dev-funding/LP-unlock entry gates, the insider-dump and LP-unlock exit feeds, the narrative/regime de-risk path, and the soft-tier entry pause are ALL inactive at once. Only the in-process ExitEngine hard stop and the circuit-breaker hard trip are live. Safe only because zero capital; every one must be wired and proven before any real-capital path — not incrementally after.
- Forced/emergency exit paths book realized PnL as literally 0 (fast_loop _do_full_exit/_force_exit/emergency_flatten_all/enforcer-stop all transition_to_closed with realized_pnl_net_lamports=0); no realized true-up PnLEvent reaches the CircuitBreaker on close, so on a real-price path exit slippage beyond the last observed mark goes unbooked. Only observationally noted, never validated.
- Constant-time auth comparison — the control plane uses plain != on operator and CEO tokens (the LIVE real-capital enable gate); hmac.compare_digest is not used. Defense-in-depth gap, unaddressed.
- CI assertion that a severity=critical Alertmanager route never terminates at a null receiver (amtool) — absent; the all-null paper config could silently ship into R3 and be mistaken for healthy.

## Honest bottom line
The system is in the correct, honest posture — real capital is DISABLED and the edge is a genuine NO-GO on launch and momentum data. That NO-GO is the edge proof working (an n=497 false-positive killed by n=4,187), not a bug, and it is the single most important true fact about the system. The safety controls that actually matter are verified sound: the circuit-breaker hard trip, the dead-man's switch as a true fail-closed separate failure domain, the leak-guarded decision/outcome boundary, the conservative realizable-exit invariant, and the asymmetric-trust shape of every LLM/sentiment/operator path (nothing automated can add risk). NONE of the confirmed findings is exploitable in the current paper/SHADOW/DRY_RUN state. But the system is NOT ready for any step toward real capital, and there are two REDs plus one cross-cutting must-fix that gate it. RED-1: the ADR-0009 custody enforcer — the one control that bounds unbounded loss — does not exist (aats-signer is a scaffold; the venue even defaults to mock signer AND mock RPC clients), so the entire blast-radius guarantee is currently vapor. RED-2: the operator dashboard control surface controls nothing against a real backend (no auth header, no proxy, no CORS → 401/CORS-blocked) and actively lies in its default mock build (green 'connected' + success toasts while doing nothing) — it must not be trusted as a control surface, though the autonomous spine and Telegram channel provide real de-risk independently. The cross-cutting capital-licensing gate: the edge proof is computed IN-SAMPLE (iid bootstrap, no purge/embargo/walk-forward on the real corpus — the walk-forward engine is wired only to synthetic data), and it is biased toward the capital-unlocking PASS by momentum's ~64s timing optimism and an uncalibrated 150bps adverse-selection floor — so no real-data GATE-B PASS may license capital until run_edge_proof scores through a purged/embargoed (and, for the correlated reaction pivot, clustered/block-bootstrap) walk-forward. The real danger is not today's zero-capital paper bot; it is the dense set of gaps — mock-client and dev-token defaults, DEVNET-to-mainnet-via-env-string, phantom-fill LIVE reconciliation, null-sink alerting, false-assurance env-name splits, incomplete tighten-only guards, and an uncertified reaction harness merged mid-audit — that would silently become load-bearing the instant someone flips toward LIVE. Keep DRY_RUN authoritative, provision no key, pin any sign-off to a frozen commit, and treat the custody enforcer, the LIVE execution-correctness cluster, the operator/alerting surfaces, and the walk-forward edge proof as hard prerequisites — all four proven with tests — before any real-capital path is even discussed."

## Appendix — all confirmed findings
### [RED] A-blockchain-core · rust/aats-signer (custody / ADR-0009)
**Issue:** aats-signer is a scaffold, not the ADR-0009 un-bypassable custody enforcer: no wallet secret, no signing, no Unix-socket listener — only GET /health. The per-tx lamport cap + program/tip allowlist that ADR-0009 designates as the one control against unbounded loss does not exist anywhere; the Python side has no independent (out-of-process) cap, and the venue defaults signer_client to MockSignerClient().
**Adjudication:** CONFIRMED. rust/aats-signer/src/main.rs:1-18 and Cargo.toml:5,14-31 self-label SCAFFOLD PLACEHOLDER; main.rs binds only METRICS_PORT/health (75-101) and does not open SIGNER_SOCKET_PATH. jito_jupiter_venue.py:180 `self._signer = signer_client or MockSignerClient()`. pretrade_gate.py:17,342 documents veto-only ('a gate cannot add risk … sizes/forces nothing'), so it cannot cap notional. Sizing is in-process: snipe_loop.py:323-339 -> sizing.py per_trade_cap_lamports (327) is an in-process config cap, NOT the independent enforcer ADR-0009 requires. Fails-closed IS real: signer_client.py:204-209 turns an unserved socket into SignerRefused('signer_unavailable'), caught at jito_jupiter_venue.py:362-367 -> trade aborts. Three DRY_RUN gates (submit_mode 214-218, _dry_run_env_disabled 1060-1066, live_submit_enabled 1037-1044) block LIVE today. Control is ABSENT, not merely weak — correct as the #1 pre-capital blocker; keep DRY_RUN and remove the MockSignerClient default so LIVE fails loudly.

### [RED] D-frontend-governance · Dashboard operator control-plane integration — auth + CORS (MISSED by auditor)
**Issue:** In the default docker LIVE build (VITE_USE_MOCK=false, VITE_CONTROL_PLANE_URL=http://localhost:8787) the dashboard's operator write path cannot reach or authenticate to the control plane: postJSON sends no Authorization: Bearer operator token, the dashboard nginx is static-only with no /api proxy to inject one, and the control plane configures no CORS. Every operator POST (kill, flatten, flatten/{mint}, breaker/reset, mode) 401s (missing Bearer) and is CORS-preflight-blocked cross-origin; cross-origin GET reads and the /api/feed EventSource are likewise CORS-blocked.
**Adjudication:** CONFIRMED by code. api.ts:157-165 postJSON sets only content-type — no Authorization/X-CEO-Auth. server.py:435-446 _check_operator_auth requires a valid Bearer on every POST → 401 (fails even when OPERATOR_TOKEN is empty). docker/nginx.conf:1-27 serves the SPA + /health only, with no location proxying /api/* to aats-controlplane. Grep across aats/ finds no CORSMiddleware/allow_origins/Access-Control anywhere, yet compose (docker-compose.yml:311-329) + Dockerfile.dashboard:36-39 wire the browser at origin :3000 directly to :8787 cross-origin. Net effect: the operator KILL/FLATTEN buttons are non-functional against the real backend. Combined with Finding 1 (mock no-op), pressing KILL never actually de-risks a real agent in ANY current build — mock lies (green success), real fails (401/CORS). Rated RED as a contract/reliability break of the operator de-risk surface. Mitigants (not a live-capital breach today): LIVE is fully server-gated, DRY_RUN is default, and the autonomous circuit-breaker/dead-man/survivable-stop spine fires independently of the dashboard; real mode also fails loud (error toast) rather than silently. Fix: front /api/* with a same-origin reverse proxy that injects OPERATOR_TOKEN server-side (browser must not hold the secret), or a BFF; add CORS if cross-origin is intended; gate destructive buttons on a live /api/state probe. Must be resolved before the dashboard is trusted as a control surface for any real-capital run.

### [YELLOW] A-blockchain-core · execution / mev — Jito bundle composition (FR-040)
**Issue:** The atomic-buy Jito bundle is orphaned: JitoBundleSubmitter.submit_atomic_buy / AtomicBuyBundle are fully built but never called by the buy path. The real entry sends a single tx (with a System-transfer tip to a Jito tip account) via plain RPC sendTransaction, not sendBundle — so the tip buys no block-engine priority/MEV protection, and the venue docstring's 'Jito bundle … reverts whole' does not describe the code.
**Adjudication:** CONFIRMED. grep: AtomicBuyBundle/submit_atomic_buy referenced only in aats/mev/bundle_submitter.py + aats/mev/__init__.py export — no construction in the execution path. snipe_loop.py:482 -> venue.execute() -> _land_with_retry_entry (833) -> _send_once (794) -> SolanaRpcClient.send_transaction (rpc_client.py:694, plain sendTransaction). Tip instruction appended at tx_builder.py:270-281. Nuance/correction: (a) the finding's paths are aats/mev/bundle_submitter.py (not aats/execution/); (b) all four instructions ride in ONE VersionedTransaction (tx_builder.py:284), so single-tx atomicity already makes the tip revert with the swap — the accurate harm is WASTED tip spend (zero MEV/priority benefit) on SUCCESSFUL lands, not loss-on-revert. Docstring claim of a 3-ix bundle with a separate assert_min_out is inaccurate (min_out is packed into swap_data at tx_builder.py:263). Must be resolved before LIVE; no capital at risk in DRY_RUN.

### [YELLOW] A-blockchain-core · execution — mainnet LIVE land/reconcile confirmation
**Issue:** On mainnet LIVE, send_transaction returns reason='landed' with land_slot=None the instant the RPC accepts the tx, with NO getSignatureStatuses poll. reconcile() treats 'landed' as a confirmed fill and poisons the idempotency set, so a submitted-but-dropped tx is recorded as a filled position and never retried — the opposite of the safer devnet path.
**Adjudication:** CONFIRMED. rpc_client.py:694-706 (SolanaRpcClient.send_transaction) returns submitted=True/reason='landed'/land_slot=None immediately; comment 'Confirmed async via getSignatureStatuses' is aspirational — no poll exists. reconcile() at jito_jupiter_venue.py:1004 sets reason_is_confirmed for 'landed', 1006 marks landed=True, execute() 342-343 adds intent to _submitted_intent_ids only on fill.landed (so a phantom 'landed' DOES poison). Devnet path is correct: send returns 'submitted_pending_confirm' (rpc_client.py:552-557) then confirm_transaction polls getSignatureStatuses (567-600). Real capital-correctness bug the moment LIVE is enabled; unreachable today (LIVE triple-gated).

### [YELLOW] A-blockchain-core · ingestion — WS keepalive / stale-connection detection (liveness)
**Issue:** Both the Helius logsSubscribe path and the PumpPortal feed set ping_interval=None (needed for free-tier servers) but neither wraps recv in an application-level read timeout. A silent half-open socket blocks async-for indefinitely: the Helius idle watchdog only evaluates on frame arrival, and the PumpPortal loop has no idle watchdog at all, so launch detection can stall until OS TCP keepalive trips.
**Adjudication:** CONFIRMED. transport.py:969-970 (Helius) and 1698-1699 (PumpPortal) set ping_interval=None/ping_timeout=None. Helius `async for raw_msg in ws` at 1009 has no asyncio.wait_for; the elapsed>poll_idle_timeout_s check at 1031-1040 is inside the loop and only runs when a (non-notification) frame arrives — it cannot fire on a fully silent socket. PumpPortal `async for` at 1713 has neither a read timeout nor any idle-elapsed check (1713-1737). Detection-redundancy/liveness gap; degrades launch detection, no capital at risk in paper/SHADOW. Fix: asyncio.wait_for(ws.recv(), timeout=poll_idle_timeout_s) or a watchdog task on both transports.

### [YELLOW] A-blockchain-core · execution/mev — Jito bundle composition (FR-040)
**Issue:** CONFIRMED. The atomic-buy Jito bundle is orphaned; the live buy path burns the tip via plain RPC with no atomicity.
**Adjudication:** Repo-wide grep for AtomicBuyBundle/submit_atomic_buy/send_bundle returns matches ONLY in aats/mev/bundle_submitter.py, aats/mev/__init__.py (export), and tests/mev/* — nothing in the execution path constructs a bundle. The real buy path is snipe_loop.py:482 self._venue.execute() -> _land_with_retry_entry -> _send_once -> SolanaRpcClient.send_transaction (rpc_client.py:694 a plain JSON-RPC sendTransaction, not sendBundle). tx_builder.py:270-281 still appends a System-transfer tip to a Jito tip account into that plain-RPC tx. A tip to a Jito tip account submitted via ordinary RPC (not through the block engine as a bundle) buys zero atomicity and zero MEV protection — on a mainnet LIVE run it is a pure loss of the tip lamports. The venue docstring's 'the Jito bundle [buy, assert_min_out, tip_ix] reverts whole' does not match the code. No capital at risk in paper/DRY_RUN; must be resolved (wire JitoBundleSubmitter.submit_atomic_buy OR drop the tip ix from the plain-RPC path) before LIVE.

### [YELLOW] A-blockchain-core · execution — mainnet LIVE land/reconcile
**Issue:** CONFIRMED. Mainnet LIVE records an RPC-accepted-but-unconfirmed tx as a filled position (phantom fills).
**Adjudication:** SolanaRpcClient.send_transaction (rpc_client.py:694-706) returns submitted=True, reason='landed', land_slot=None the instant sendTransaction returns a signature (acceptance, NOT landing) — its own comment says 'Confirmed async via getSignatureStatuses' but no such poll exists on the LIVE path. execute() calls reconcile() immediately (venue:337) and reconcile treats reason in ('landed','devnet_landed') as a confirmed fill (venue:1004-1015) -> fill.landed=True -> intent added to the idempotency set (venue:342-343). The DEVNET path does it correctly (send returns then _send_and_confirm_devnet polls confirm_transaction/getSignatureStatuses, venue:702-792) — the mainnet path skips exactly that. A submitted-but-dropped mainnet tx becomes a phantom position and is never retried; position/OMS desync results. Compounding note (not in the original finding): the idempotency set self._submitted_intent_ids (venue:198) is in-memory only, so a process restart after a phantom fill loses dedup entirely. Not exercised today (LIVE unreachable); a capital-correctness bug the moment LIVE is enabled.

### [YELLOW] A-blockchain-core · ingestion — WS keepalive / stale-connection detection
**Issue:** CONFIRMED. Both WS transports disable pings and lack an application-level read timeout, so a silent half-open socket stalls detection.
**Adjudication:** Helius logsSubscribe (transport.py:969-970 ping_interval=None, ping_timeout=None) and PumpPortal (transport.py:1698-1699 same) both then do a bare `async for raw_msg in ws` (lines 1009 and 1713) with no asyncio.wait_for read timeout. The Helius idle watchdog (1032-1040) computes `elapsed` and returns ONLY inside the loop body when a non-notification frame arrives — on a truly silent socket no frame arrives, so it can never fire. The PumpPortal loop has no idle watchdog at all. A dead-but-not-closed WS therefore blocks until OS TCP keepalive trips (minutes-hours), stalling launch detection with no reconnect. Fix: wrap the recv in asyncio.wait_for(..., timeout=poll_idle_timeout_s) on both transports to force the existing exponential-backoff reconnect.

### [YELLOW] A-blockchain-core · backtest — real-corpus edge proof (GATE-A/GATE-B) OOS integrity
**Issue:** MISSED by the 7-finding set. The real-corpus edge verdict is computed IN-SAMPLE — the purged/embargoed walk-forward is applied only to a synthetic corpus, never to the real one.
**Adjudication:** run_edge_proof.py:187-189 computes compute_gate_a and compute_gate_b_delta on the ENTIRE resolved TradeOutcome list from build_from_corpus / build_momentum_from_corpus — no train/test split, no purge, no embargo, no walk-forward. gate_b.compute_gate_b_delta (gate_b.py:299-316) resamples the SAME dataset with replacement, so its lower-95 bootstrap bound measures within-sample sampling variability, NOT out-of-sample generalization. A real purged+embargoed walk-forward DOES exist (tests/validation/harness.py: purged_embargoed_windows + assert_purge_is_load_bearing) but is exercised only against aats.models.synthetic.SyntheticRow / generate_synthetic_corpus (note the IS_BOOTSTRAP_NOT_REAL import) in tests/validation/test_edge_gate_proof.py:41-310 — it is never wired into the real recorded-corpus proof. This violates the charter's OOS/purge/embargo edge bar: an in-sample GATE-B PASS on the real corpus (the momentum 'first GATE-B PASS' per project memory) does not clear the bar. Blast radius is limited TODAY because both live models are FROZEN, hash-guarded, unfitted rules (thin_model_selected; momentum buy_fraction/liquidity floors in MOMENTUM_PARAMS.frozen.json) with no parameter estimation on the corpus — but nothing structurally prevents a catastrophic in-sample leak the moment the trained ONNX classifier (SnipeInference) plugs into THIN_MODEL_SEAM and is scored on the corpus it was trained on. Verified this session: 103 tests passed in 43.20s across tests/validation + tests/backtest. Fix: route the real corpus through purged_embargoed_windows and report per-fold OOS GATE-B with CIs before any trained-model verdict is trusted for a capital gate.

### [YELLOW] B-trading-safety · Catastrophic exit producers (E14 insider-dump, E19 LP-unlock, narrative)
**Issue:** CONFIRMED. Exit-side plumbing is correct and e2e-tested (test_e2e_catastrophic_exits.py, 8 tests pass) and E17 sellability re-probe is fully LIVE (sellability_reprober wired __main__.py:475, driven by slow_enrichment_task). But E14 insider/dev-sell never fires (no live SELL feed), E19 LP-unlock never fires (lp_unlock_schedule_source=None), and narrative_failure is unfed (reasoner=None).
**Adjudication:** Verified: transport.py:1793-1828 PumpPortalTransport routes only txType create->CREATE and migrate->WITHDRAW; buy/sell are explicitly '(not subscribed) -> skip'. orchestrator.on_decoded_sell exists (loops.py:173) but is never invoked in __main__.py. __main__.py:482 sets lp_unlock_schedule_source=None; __main__.py:426 sets slow_loop reasoner=None. So only ~1 of ~4 open-position catastrophic signals actually fires. Honestly TODO(M1)-marked (__main__.py:455-488). Data-ingestion dependency, correctly rated YELLOW.

### [YELLOW] B-trading-safety · Circuit breaker soft-tier (AUDIT-risktiers) on entry path
**Issue:** CONFIRMED. The soft REDUCE/PAUSE tier is dormant on the running entry path. snipe_loop.py:238-246 reads only the projected BreakerState.state == 'TRIPPED'; it never consults breaker.entries_allowed()/risk_posture(), and SnipeLoop holds no CircuitBreaker reference. New entries are NOT paused in the -2%..-3% soft band.
**Adjudication:** Verified: the soft posture is DERIVED internally (circuit_breaker.py:424-434 _posture from _soft_reduced_days + _current_day) and is NOT a BreakerState contract field, so the StateStore projection (fast_loop.py:412 save_breaker_state) keeps state=='ARMED' during soft-reduce. The only consumer of entries_allowed() is resting_orders.py:597, which is not imported/wired in __main__.py. Fails safe (the hard -3% trip still fires and is respected). YELLOW correct.

### [YELLOW] B-trading-safety · Leak audit: momentum harness point-in-time (T-300a)
**Issue:** CONFIRMED. The momentum decision reads forward marks stamped at NOMINAL horizons (launch_block_time + horizon_s*1000), but the collector samples the nominal-60s mark at ~64s median (p90 +15.6s). assert_features_leq_decision binds against the nominal stamp, so it cannot catch that the mark's true information content is ~4s after the nominal cutoff. Mild forward-information optimism biasing GATE-A/GATE-B toward PASS (the capital-unlocking direction).
**Adjudication:** Verified: momentum_harness.py:412-442 assemble_momentum_features stamps each forward feature at et = bt + o.horizon_s*1000 (nominal); MomentumForwardObservation carries obs_wall_ms (the true observation time) but it is NEVER used for the feature event-time. The leak boundary is structurally intact (this is staleness/latency-optimism, not outcome lookahead), and it is honestly self-documented (momentum_harness.py:55-68) with the fix assigned to the ingestion lane. YELLOW correct; deserves attention because the momentum strategy is the one that PASSED GATE-B, so the optimism is in the dangerous direction.

### [YELLOW] B-trading-safety · Catastrophic exit producers (E14 insider-dump, E19 LP-unlock, narrative)
**Issue:** E17 delayed-honeypot re-probe is fully live, but E14 insider/dev-sell dump never fires (no live SELL feed; on_decoded_sell never called), E19 LP-unlock never fires (lp_unlock_schedule_source=None), and narrative_failure is unfed (reasoner=None, no regime_wiring). Only 1 of ~4 open-position catastrophic signals actually fires.
**Adjudication:** CONFIRMED. __main__.py enrichment block (474-488) sets lp_unlock_schedule_source=None and logs E17=LIVE, E14=WIRED_NO_FEED, E19=WIRED_NO_SOURCE. orchestrator.on_decoded_sell exists (loops.py:173) but is referenced in __main__ only as a comment (line 468); pumpportal_feed_task forwards launches via on_launch_event only. SlowLoop is built with reasoner=None (:426) and ControllerOrchestrator (:490-499) is constructed without regime_wiring (defaults None per loops.py:70). Exit branches themselves are E2E-tested; this is an honestly TODO(M1)-marked upstream-feed dependency. YELLOW appropriate.

### [YELLOW] B-trading-safety · Circuit breaker soft-tier (AUDIT-risktiers)
**Issue:** The soft REDUCE/PAUSE tier (~-2% before the hard -3% trip) does not pause new entries on the running path. The SNIPE loop reads only the projected BreakerState.state=='TRIPPED'; the soft posture is in-process-only and never projected, so entries are not paused in the -2%..-3% band.
**Adjudication:** CONFIRMED. snipe_loop.py:238-246 reads store.load_breaker_state() and checks state=='TRIPPED' only; SnipeLoop holds no CircuitBreaker reference and never calls entries_allowed(). Grep shows entries_allowed() is consulted only in resting_orders.py:597 (not imported/wired in __main__) and tests. circuit_breaker.record_pnl leaves BreakerState.state=='ARMED' during soft-reduce (posture is derived from in-process _soft_reduced_days + _current_day, not persisted), and fast_loop projects that ARMED state into the store. Fails safe — the hard trip still fires and is respected. YELLOW appropriate.

### [YELLOW] B-trading-safety · Leak audit: momentum harness nominal-vs-true horizon stamping
**Issue:** Forward marks are stamped at NOMINAL event-time (launch_block_time + horizon_s*1000), but the collector samples the '60s' mark at ~64s median (p90 +15.6s). The leak guard binds against the nominal cutoff, so ~4s of extra trajectory information enters the T_ENTRY decision — mild forward-information optimism biasing GATE-A/GATE-B toward PASS.
**Adjudication:** CONFIRMED. momentum_harness.py:434 stamps each pre-entry mark feature at et = bt + o.horizon_s*1000 (nominal). obs_wall_ms IS carried on MomentumForwardObservation (:340) but is not used for the stamp, so assert_features_leq_decision cannot bind against the true ~64s observation time (self-documented at docstring lines 55-68). The optimism is in the PASS/capital-unlocking direction. Structural single-trade decision/outcome separation is otherwise intact. YELLOW appropriate; fix is the ingestion lane's.

### [YELLOW] B-trading-safety · MISSED — Edge gate is in-sample iid bootstrap, not purged/embargoed walk-forward on the REAL corpus
**Issue:** The runnable capital-licensing proof (run_edge_proof over the real momentum/reaction/launch corpus) computes GATE-B via compute_gate_b_delta as an iid trade-resample bootstrap over the ENTIRE outcome list — no temporal train/test split, no purge of horizon-overlapping labels, no embargo. The purged+embargoed walk-forward engine exists but is exercised ONLY on the synthetic bootstrap corpus. Under correlated real outcomes the lower-95 bound is optimistic — biased toward the PASS that unlocks capital.
**Adjudication:** CONFIRMED (new). run_edge_proof.py:207-215 calls compute_gate_a/compute_gate_b_delta over all outcomes with no windowing. purged_embargoed_windows (tests/validation/harness.py:201) operates on SyntheticRow and is used only by tests/validation/test_edge_gate_proof.py, whose corpus is generate_synthetic_corpus (IS_BOOTSTRAP_NOT_REAL=True; header line 21: 'DOES NOT license capital'). Real meme-launch outcomes are strongly correlated (shared market regime; momentum holds walk marks out to 1800s = 30 min, so adjacent launches' label horizons overlap), so an iid bootstrap understates variance and the lower-95 bound is optimistic — exactly the direction that must never be optimistic for a capital gate. Per the charter ('in-sample edge is no edge; purged + embargoed walk-forward or it doesn't pass'), no GATE-B PASS on real data is a valid capital-unlocking verdict until run_edge_proof scores through the purged/embargoed walk-forward path. Mitigations: point estimate uncorrupted; selection rules are frozen a priori (no parameter overfit); DRY_RUN and the prior real-data verdict was NO-GO. YELLOW, but it BLOCKS any real-data GATE-B PASS from licensing capital.

### [YELLOW] C-platform-infra · control_plane / docker-compose (kill-switch exposure)
**Issue:** CONFIRMED. docker-compose.yml:214-225 publishes the destructive control plane on 0.0.0.0:8787 (ports "8787:8787" + CONTROL_PLANE_BIND_HOST=0.0.0.0) but injects only CEO_AUTH_TOKEN (216) — a name the code does not read. The container runs python -m aats.control_plane.app (Dockerfile.controlplane:62) -> server.py ControlPlaneConfig, where operator_token defaults to "dev-token" (server.py:76) and ceo_token to "" (server.py:78). Same default in control_api.py:107 and controller/__main__.py:507. Compose passes env only via the environment: block (no env_file:), so POST /api/kill,/flatten,/breaker/reset,/risk-config,/mode are gated only by the public default Bearer dev-token, reachable from every host interface. Loopback hardening (deploy/docker-compose.controlplane-bind.override.yml) is opt-in.
**Adjudication:** Verified via docker-compose.yml, Dockerfile.controlplane, server.py, control_api.py, __main__.py, app.py, .env.example. Severity YELLOW is defensible because capital is fenced: DRY_RUN_ENABLED default true and _check_ceo_auth (server.py:451) is fail-closed on empty ceo_token, so this is paper-bot/breaker-reset DoS-and-griefing, not a real-capital path. ADD (auditor under-weighted): because compose injects the WRONG env names, an operator who sets OPERATOR_TOKEN/CEO_TOKEN in .env has them silently dropped — the container keeps accepting dev-token, giving false assurance, and the documented LIVE-enable path can never function. I concur with 'promote to RED before any host exposure or LIVE'.

### [YELLOW] C-platform-infra · monitoring / Alertmanager (dead-man-switch alerting)
**Issue:** CONFIRMED. monitoring/prometheus/rules/aats.yml emits the P1 rules with severity: critical (ModuleHeartbeatDead:17, DMSHeartbeatStaleCritical:31, CircuitBreakerTripped:59, PositionDriftDetected:125) — they fire in Prometheus. But alertmanager.yml routes root (26), severity=critical (33) and severity=warning (39) all to receiver default-null (48), an empty sink. Nothing is dispatched to Telegram/PagerDuty in the shipped stack; AC-052/053 (<=10s P1 push) is inert until R3 credentials land.
**Adjudication:** Verified against alertmanager.yml and aats.yml. Reinforcement: the compose alertmanager service (docker-compose.yml:405-418) sets command: only, with no entrypoint: envsubst wrapper, so even the injected ALERTMANAGER_TELEGRAM_* env vars are never consumed — the pipeline is doubly silent. An operator would see green Grafana panels while every P1 page is dropped.

### [YELLOW] C-platform-infra · docker-compose / supply-chain + Grafana default cred
**Issue:** CONFIRMED. No @sha256: digest on any third-party image: redis:7.2.5-alpine (67), prom/prometheus:v2.53.0 (347), grafana/grafana:11.1.0 (375), prom/alertmanager:v0.27.0 (405) — despite in-file comments (29-40, 341-345, 372-373, 402-403) asserting the digests are present/'VERIFIED'/'documented ... below'. Grafana ships GF_SECURITY_ADMIN_PASSWORD:-changeme (379) on 0.0.0.0:3001 (387).
**Adjudication:** Verified against docker-compose.yml. Tag-only pinning means a re-pushed tag silently changes the image; the comments overstate the actual state. Flagged as an R3 pre-live checklist item, so a documented gap rather than a surprise — YELLOW is correct.

### [YELLOW] C-platform-infra · control_plane / docker-compose (kill-switch exposure)
**Issue:** Default `docker compose up` publishes the destructive de-risk/kill control plane on 0.0.0.0:8787 with operator auth falling back to the hardcoded default `dev-token`; compose injects only CEO_AUTH_TOKEN (unread) — never OPERATOR_TOKEN/CEO_TOKEN.
**Adjudication:** CONFIRMED. server.py:76 `operator_token` default_factory = env OPERATOR_TOKEN else "dev-token"; :78 `ceo_token` default "". Same `dev-token` default at control_api.py:107 and __main__.py:507. docker-compose.yml aats-controlplane `environment:` block (216) lists ONLY CEO_AUTH_TOKEN — Compose does NOT auto-inject host env into containers, so OPERATOR_TOKEN never reaches the process => auth token IS `dev-token`. Bind is 0.0.0.0 (compose:223) and Dockerfile.controlplane CMD `python -m aats.control_plane.app` -> resolve_bind_host() honors CONTROL_PLANE_BIND_HOST=0.0.0.0 (app.py:99-113, warns but binds); ports 8787:8787 (compose:225) publishes on all host interfaces. Capital IS fenced: DRY_RUN default true (compose:51) and LIVE fail-closed because ceo_token="" (server.py:451 `not cfg.ceo_token` -> 403) AND dry_run gate (server.py:1124). Worst reachable action with dev-token = DoS via kill/flatten (both de-risk/safe) + reset a TRIPPED paper breaker + tighten risk — no real-capital path. Name split confirmed in .env.example (CEO_AUTH_TOKEN:124 vs CEO_TOKEN:258; OPERATOR_API_TOKEN:137 vs OPERATOR_TOKEN:255). Loopback override deploy/docker-compose.controlplane-bind.override.yml exists but is opt-in. YELLOW concurred; the 'promote to RED before host-exposure/LIVE' caveat is correct.

### [YELLOW] C-platform-infra · monitoring / Alertmanager (dead-man-switch alerting)
**Issue:** Every Alertmanager route, including severity=critical (CircuitBreakerTripped, DMSHeartbeatStaleCritical, ModuleHeartbeatDead, PositionDriftDetected), terminates at the empty `default-null` receiver — no page is delivered in the shipped stack.
**Adjudication:** CONFIRMED. alertmanager.yml top route receiver='default-null' (26); the critical matcher route also 'default-null' (33); warning route 'default-null' (39); only receiver defined is `default-null` (48), live telegram/pagerduty receivers are commented out (53-83). The Prometheus rules DO fire — monitoring/prometheus/rules/aats.yml defines all four P1 alerts with severity=critical (lines 13,29,55,121). So AC-052/053 (P1 push <=10s) is inert until R3 creds. Safety-relevant nuance in my lane: the DMS itself still FIRES independently of alerting (dms_service.py check()), so this is a lost human-notification, not a defeated safety control — YELLOW is correct. Note the trap: ALERTMANAGER_TELEGRAM_BOT_TOKEN is env-wired (compose:413) but unused, so setting it changes nothing without also editing the yml.

### [YELLOW] C-platform-infra · docker-compose / supply-chain + Grafana default cred
**Issue:** Third-party images are tag-pinned only (no @sha256 digests) despite ~30 lines of comments asserting 'documented digests that MUST be verified'; Grafana ships GF_SECURITY_ADMIN_PASSWORD default `changeme` on 0.0.0.0:3001.
**Adjudication:** CONFIRMED. docker-compose.yml image refs carry NO @sha256: redis:7.2.5-alpine (67), prom/prometheus:v2.53.0 (347), grafana/grafana:11.1.0 (375), prom/alertmanager:v0.27.0 (405); the digest comments (29-40, 341-345, 372-373, 402-403) overstate the actual state (a re-pushed tag would silently change the image). grafana GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD:-changeme} (379), published 3001:3000 (387) i.e. 0.0.0.0. Documented R3 pre-live checklist item; monitoring UI only, not a capital path. YELLOW concurred.

### [YELLOW] C-platform-infra · control_plane / risk-config tighten-only (asymmetric-trust) — MISSED by the lane
**Issue:** POST /api/risk-config tighten-only guard has an INCOMPLETE allowlist: 6 of 14 RiskConfig fields can be WIDENED and still return 200 + persist — the endpoint whose docstring calls itself the 'ASYMMETRIC TRUST GUARD' does not guard them, and no test covers the gap.
**Adjudication:** NEW, CONFIRMED by command this session. _validate_risk_config_tighten_only (server.py:270-333) checks only per_trade_cap_lamports, max_aggregate_lamports, max_slippage_bps, jito_tip_cap_frac, kelly_fraction_cap, daily_loss_limit_pct, snipe_threshold. I ran it with current->proposed widening t_dms_seconds 60->999999, blast_radius_cap_lamports 1e8->9e9, n_wallets_max 1->5, daily_loss_floor_lamports 3e8->9e9, veto_threshold 0.30->0.0, daily_risk_tranche_lamports 5e8->9e9 => violations reported: []. The subsequent RiskConfig(**body) model validator (contracts/risk.py _floor_invariants) also does NOT constrain any of these 6 => I constructed the widened RiskConfig successfully => the full POST returns 200 and _set_current_risk_config persists it (server.py:926). t_dms_seconds (DMS deadline), daily_loss_floor_lamports (breaker floor) and veto_threshold (LLM veto sensitivity) are directly safety-relevant widens. SEVERITY BOUNDED TO YELLOW because grep confirms _risk_config_box / _set_current_risk_config are referenced ONLY inside server.py's GET/POST handlers, __main__.py passes no risk_config_provider, and the live breaker/DMS read their own RiskConfig/env — so today the persisted widen is display-only with no live effect. It is a latent asymmetric-trust contract hole (contradicts the stated guarantee, untested) that becomes load-bearing the moment anyone wires the box to the live gate. File as a defect against the strategy/control-plane owner: extend the tighten-only allowlist (or reject any field absent from it) and add a widen-trap test for all safety fields.

### [YELLOW] D-frontend-governance · Dashboard / operator kill-flatten controls (Finding 1)
**Issue:** USE_MOCK defaults TRUE; in mock mode killSwitch/flattenAll/flatten/resetBreaker are local no-ops and setMode mutates only local mock state, while KillSwitch renders a success toast — no runtime MOCK/DISCONNECTED indicator anywhere in the UI.
**Adjudication:** CONFIRMED. dashboard/src/lib/api.ts:93-94 sets USE_MOCK = (VITE_USE_MOCK !== 'false') → default true. api.ts:878-908 each destructive fn does `if (USE_MOCK){ await sleep(150); return; }`; setMode (api.ts:928-935) calls setMockMode(m) and returns. KillSwitch.tsx:29-43 awaits killSwitch() then unconditionally toasts 'Kill switch engaged' on resolve. Grep confirms USE_MOCK is imported only by a test (pages.render.test.tsx) — no component, ModeBadge, or CommandDeck surfaces it; components grep for mock/disconnect/offline yields only test files. Mitigant is real: LIVE is server-gated and DRY_RUN is default, and the autonomous safety spine is server-side, so no active capital breach today. YELLOW stands; must become fail-loud before the dashboard is relied on as a real control surface.

### [YELLOW] D-frontend-governance · Control-plane governance ladder /api/mode (Finding 2)
**Issue:** The LIVE/CEO gate fires only at the LIVE rung; upward staging moves SHADOW→PAPER→LIVE_DRY_RUN are accepted with operator Bearer token alone, contradicting the file header's 'Every operator command may only DE-RISK'.
**Adjudication:** CONFIRMED. server.py:1122 guards `if not _mode_is_downward(current, proposed) and proposed == 'LIVE'` — the CEO/DRY_RUN checks (1124-1137) are reachable only when proposed=='LIVE'. _MODE_ORDER (server.py:340) with _mode_is_downward (343-345) means any upward-but-not-LIVE proposal skips the guard and is applied after only _check_operator_auth (1104). Header line 13 asserts de-risk-only; line 30 partially documents the exception ('LIVE is fenced; moving down is always allowed'), so this is a spec-vs-header consistency gap, not a capital breach (LIVE_DRY_RUN still honors DRY_RUN). YELLOW accurate.

### [YELLOW] D-frontend-governance · Dashboard / operator kill-flatten controls
**Issue:** CONFIRMED: USE_MOCK defaults TRUE (api.ts:93-94) and all destructive actions killSwitch/flattenAll/flatten/resetBreaker/setMode no-op-and-return in mock mode (api.ts:878-935). USE_MOCK is never surfaced in any UI component (grep: imported only by tests); Layout.tsx:84-197 drives LiveDot/Geyser/ShredStream/ModeBadge from useAgentState(), which in mock returns geyser=true/shredstream=true — so the dashboard actively renders a GREEN/CONNECTED banner while every control is a fake success. An operator opening a real agent without VITE_USE_MOCK=false gets a success toast on KILL while the agent keeps trading.
**Adjudication:** Verified by direct read of api.ts, Layout.tsx, and a project-wide grep. The finding understates it slightly: the connection indicators are fabricated-state-driven, not USE_MOCK-driven, so they show green in mock. Severity YELLOW is correct: the autonomous server-side safety spine (circuit breaker, dead-man switch, survivable stop) and the token-carrying Telegram operator channel both fire independently of the dashboard, so only the dashboard-initiated de-risk path gives false assurance. Recommendation stands: fail-loud MOCK/DISCONNECTED banner + probe /api/state on load + gate destructive buttons on a live connection.

### [YELLOW] D-frontend-governance · Control-plane governance ladder (/api/mode)
**Issue:** CONFIRMED: post_mode (server.py:1092-1147) applies the DRY_RUN_ENABLED=false + CEO-auth gate ONLY at the LIVE rung — guard is `if not _mode_is_downward(current, proposed) and proposed == "LIVE"`. With _MODE_ORDER = SHADOW<PAPER<LIVE_DRY_RUN<LIVE, upward staging moves (SHADOW->PAPER->LIVE_DRY_RUN) are accepted with only the operator Bearer token — no CEO auth, no confirmation. The file header (server.py:13) asserts 'Every operator command may only DE-RISK', which upward moves contradict.
**Adjudication:** Verified _MODE_ORDER (server.py:340) and the guard (1122). Confirmed NOT a capital breach: controller/__main__.py:66-78 hard-refuses to run when DRY_RUN_ENABLED=false, and real-vs-sim venue selection is keyed off that boot flag, not agent_mode; LIVE (the only real-capital rung) requires DRY_RUN_ENABLED=false AND CEO auth. So this is a spec/header-vs-implementation consistency gap, not a real-capital exposure. YELLOW is the correct severity. Fix: either document the intentional sub-LIVE staging exception in the header/api-contracts, or gate upward staging moves behind an explicit confirmation so 'de-risk only' reads true end to end.

### [YELLOW] D-frontend-governance · Dashboard control-plane auth (operator de-risk POST path)
**Issue:** MISSED by the original set. Dashboard postJSON (api.ts:157-165) sets only content-type — it sends NO Authorization header. The server requires operator Bearer auth on EVERY POST (_check_operator_auth). No auth token exists anywhere in dashboard/src (grep) and vite.config.ts has no proxy injecting one. Net effect in live mode (VITE_USE_MOCK=false) against the real control plane: every operator de-risk command (kill/flatten/mode/breaker-reset) returns 403 -> error toast; the dashboard KILL/FLATTEN buttons are NON-FUNCTIONAL unless a fronting reverse proxy injects the operator token (an undocumented deployment dependency). api.destructive.test.ts asserts endpoint+method but never asserts an auth header, so the gap is untested.
**Adjudication:** Confirmed by reading api.ts postJSON, a project-wide grep for Authorization/Bearer/token (only coin-name data fields matched), vite.config.ts (no server.proxy), and api.destructive.test.ts (fetch mock returns 200 for all, headers never inspected). This is the complement of Finding #1: mock mode = silent fake success; live mode without proxy = fail-loud 403. It also undercuts Finding #6's framing of the missing header as purely a LIVE-safety feature. Severity YELLOW (not RED): the autonomous server-side safety spine and the token-carrying Telegram HttpControlPlaneClient both provide working operator/automatic de-risk, and the live-mode failure is fail-loud (503/403 propagates to an error toast per api.destructive.test.ts:106-110), not silent. Fix: inject the operator token (env-provided at build/deploy or proxy-injected) and add a test asserting the Authorization header is present on every destructive POST; document the reverse-proxy auth dependency.

### [YELLOW] E-security-edge · Custody / signer enforcer (ADR-0009)
**Issue:** CONFIRMED: aats-signer is a SCAFFOLD, not the real caller-independent spend-cap+allowlist+tip-pin enforcer. The 'enforcer is REAL' premise is refuted.
**Adjudication:** Verified by direct read. rust/aats-signer/src/main.rs:1-18 and :54-67 label it a 'SCAFFOLD PLACEHOLDER'; it serves only GET /health (main.rs:34) then loops (main.rs:84) — no key load, no Vault/mlock/zeroize, no Unix-socket listener, none of the 3 refusals. The allowlist + tip-pin exist ONLY as policy data (config/program-allowlist.json, which self-describes as 'SOURCE OF POLICY, not a hot-path decoder literal'); nothing enforces it. NOT exploitable today: no key exists, DRY_RUN default true, and SocketSignerClient.sign raises SignerRefused(reason='signer_unavailable') on OSError (signer_client.py:204-209) = fail closed. Already logged as F1/Condition C1 in EXEC-CUSTODY-AUDIT-2026-07-06.md:40,73-88,159-163. Keep DRY_RUN_ENABLED=true and provision no key until T-251/T-352a implements + re-audits the signer with tests proving refusal of an over-cap tx and an off-allowlist program-ID tx.

### [YELLOW] E-security-edge · Dependency supply chain
**Issue:** CONFIRMED: version-pinned but not hash-locked; no pip-audit/osv-scanner/cargo-audit in CI.
**Adjudication:** requirements/requirements.txt is fully ==-pinned (lines 14-95) — good — but there is no hash-locked lockfile and no --require-hashes install; pyproject.toml:14-52 declares >= ranges; .github/workflows/ci.yml gates are bandit+detect-secrets+grep only (lines 38-105) with test deps installed unpinned (line 146) and no dependency vuln scan or cargo audit for the future key-holding Rust crate. Matches audit F3/C4. Not exploitable in paper mode. Remediate with a --generate-hashes lockfile + pip-audit/osv-scanner + cargo audit as CI gates (go-live condition C4).

### [YELLOW] E-security-edge · Custody / signer enforcer (ADR-0009) — rust/aats-signer/src/main.rs
**Issue:** CONFIRMED. main.rs (read line 1-102) is an explicit SCAFFOLD PLACEHOLDER: banner literally says 'NO wallet secret is loaded. NO signing operations performed.' It binds only a TCP /health port on METRICS_PORT and loops; it does NOT bind the Unix-domain socket, does no Vault/mlock/zeroize, and implements NONE of the three ADR-0009 refusals (per-tx + rolling spend cap, program-ID allowlist, value-transfer recipient pin). The premise 'the spend-cap+allowlist enforcer is REAL' is FALSE.
**Adjudication:** Not exploitable today: no key exists anywhere; SocketSignerClient.sign() (signer_client.py:204-209) hits OSError on the absent socket -> raises SignerRefused(reason='signer_unavailable') -> fail-closed; DRY_RUN_ENABLED=true default. But it is the control the entire ADR-0009 blast-radius guarantee rests on and a hard go-live blocker. Keep DRY_RUN on, provision no key until T-251/T-352a implements it AND a test proves the signer refuses an over-cap tx and an off-allowlist program-ID before any key exists.

### [YELLOW] E-security-edge · Dependency supply chain — pyproject.toml / requirements / CI
**Issue:** CONFIRMED. requirements/requirements.txt is version-pinned (34 '==' lines, 0 '--hash' lines) but there is NO hash-locked lockfile (no uv.lock/poetry.lock, no --require-hashes). pyproject.toml declares '>=' ranges for the exact hot targets (solders>=0.21.0, solana>=0.35.0, httpx>=0.27.0). CI (.github/workflows/ci.yml) has no pip-audit/osv-scanner/cargo-audit/gitleaks/trufflehog/require-hashes gate (grep returned empty).
**Adjudication:** Not exploitable in paper mode, but a yanked/compromised release or dependency-confusion package could be pulled at build time, and the Rust signer crate that will eventually hold the key has no cargo-audit. Generate a --generate-hashes lockfile, install with --require-hashes, and add pip-audit/osv + cargo audit as CI gates (go-live condition).

### [YELLOW] E-security-edge · MISSED — Reaction/front-run harness (NEW, untracked) — aats/backtest/reaction_harness.py + run_edge_proof.py --strategy reaction
**Issue:** MISSED BY PRIOR AUDIT. A brand-new untracked 40KB harness (created this session, 2026-07-07 14:38) is now BUILT and WIRED into the runnable edge proof (run_edge_proof.py grew STRATEGY_REACTION -> build_reaction_from_corpus, line 168-171). It has ZERO covering tests (grep of tests/ for reaction_harness / ReputationLeakError / assert_reputation_inputs_prior / build_reaction / decide_reaction = NONE FOUND), yet its module docstring claims 'both tested RED-before/GREEN-after'. That claim is false as of this session.
**Adjudication:** On code-read the harness is leak-safe BY CONSTRUCTION: boundary-1 (signal-anchored) via assemble_reaction_features stamped at signal_block_time_ms + assert_features_leq_decision; boundary-2 (walk-forward source reputation) via assert_reputation_inputs_prior/ReputationLeakError with a strict '<' prior-signal filter AND an outcome_resolved<=decision already-observed filter, per-source isolation, first-N-neutral-prior policy; model is a strict SUBSET of the follow-every-signal baseline (de-risk-only); conservative entry-latency haircut only raises entry price; frozen REACTION_PARAMS.frozen.json hash verifies at import. Fail-closed preserved (no corpus file -> empty -> EXIT_FAIL_CLOSED). BUT per the one-law discipline an untested leak guard cannot be certified GREEN: it must ship with its leak RED/GREEN, reputation-leak RED, model-subset, and frozen-drift tests before ANY reaction GO is trusted. G3 blocker for the reaction lane; not a live risk (DRY_RUN on, no reaction corpus exists yet).

### [YELLOW] E-security-edge · MISSED — Gate confidence intervals are i.i.d. trade-level bootstraps — aats/models/gate_a.py / gate_b.py
**Issue:** MISSED BY PRIOR AUDIT. compute_gate_a and compute_gate_b_delta resample individual TradeOutcome rows i.i.d. with replacement (rng.integers(0,n,size=n)). There is NO purge, NO embargo, and NO block/time/cluster structure, despite the docstrings citing 'purged/embargoed walk-forward test windows' and a walk-forward-methodology.md spec. The lower-95 bound is thus computed under an independence assumption the data does not satisfy.
**Adjudication:** For launch/momentum (one row per mint, frozen-a-priori params => whole corpus is OOS) this is mostly moot, and the residual temporal-clustering effect only narrows the CI (cuts toward FALSE GO, not false NO-GO), so it is latent while verdicts are NO-GO. It is ACUTE for the reaction pivot: reaction signals cluster heavily in time (a hot token draws multiple whale/KOL signals within minutes; a market-wide risk-on wave lifts many at once) and the same-source reputation induces cross-signal dependence — an iid bootstrap there can manufacture lower95>0 on correlated noise. Before trusting any reaction GO, cluster the bootstrap (by mint / time-bucket / source) or use a block bootstrap.

### [GREEN] A-blockchain-core · ingestion — quote-mint guard
**Issue:** Quote-mint guard (USDC/USDT/WSOL) is correctly implemented as defense-in-depth in both create and migration mapping plus a secondary check in the recorder.
**Adjudication:** CONFIRMED. transport.py:1498-1502 QUOTE_MINTS frozenset {USDC EPjF…, USDT Es9v…, WSOL So111…112}. _map_new_token drops at 1824-1834 (events_skipped++) BEFORE LaunchEvent build; _map_migration drops at 1920-1930 (addresses the confirmed live 1/40 USDC failure mode). store.py:590 imports the same frozenset and re-checks at 602-605 (orphan_events_total++). Single shared source confirmed — no drift. No action.

### [GREEN] A-blockchain-core · ingestion — point-in-time (T-300a) + money discipline
**Issue:** T-300a point-in-time honesty and integer-money discipline are upheld: event_time.block_time_ms comes only from on-chain getTransaction; events without block_time are not yielded; wall-clock is never substituted.
**Adjudication:** CONFIRMED. transport._get_slot_time obtains (slot, block_time_ms) from RPC getTransaction and returns (None,None) when blockTime absent/null/zero (docstring at 1995+; grep confirms getTransaction call). _map_new_token 1836-1846 holds pending (_pending_no_block_time++/events_skipped++) when block_time_ms is None; wall_clock_ms carried separately (1873) and data_staleness_ms derived from block_time (1852). Money: sol_reserve_lamports int(float*1e9) (1857), token_reserve_base int() (1861). Informational (as auditor noted): 1857 briefly routes vSol through float — fine for reserve magnitudes; use Decimal if high-precision SOL strings ever appear. No action.

### [GREEN] A-blockchain-core · execution — simulate-before-send + priority-fee math
**Issue:** Simulate-before-send is enforced on every non-bundle path with simulated bytes == landed bytes; blockhash-expiry retry rebuilds/re-signs/re-simulates before each submit; DRY_RUN hard-stops before any network call; priority-fee/CU math is integer-correct; idempotency set is poisoned only on a confirmed fill.
**Adjudication:** CONFIRMED. execute(): build real keys (283) -> simulate (297) -> size_cu (300) -> rebuild sized CU (303) -> re-simulate final bytes (318) -> land (325). _land_with_retry_entry (833-976): fresh blockhash (902) -> rebuild (903) -> re-sign (912) -> re-simulate (916) -> abort on retry revert (926-931). DRY_RUN hard-stop at land() 650-665 and retry loop 859-875. _compute_priority_lamports=(cu_price*cu_limit)//1_000_000 (1147); size_cu_limit=int(Decimal(sim)*1.25) capped 400k (tx_builder.py:102-110). Idempotency poisoned only on fill.landed (342-343). Deferred-to-LIVE note is REAL and correctly non-blocking: _build_real_entry_tx passes address_lookup_table_accounts=[] (tx_builder.py:288), placeholder swap discriminator with big-endian >QQ packing (262-263), and a malformed System-transfer 'discriminator' b'\x00\x00\x00\x00\x02\x00\x00\x00' (272) — correct System transfer is a 4-byte LE u32 index (02 00 00 00) + 8-byte LE lamports, so this decodes as instruction index 0, not transfer #2. All moot in paper/stub (simulate-before-send catches it; LIVE triple-gated) but must be replaced with IDL-derived encodings + a populated ALT before LIVE. No action on simulate/fee logic.

### [GREEN] A-blockchain-core · ingestion — quote-mint guard
**Issue:** CONFIRMED CORRECT. Defense-in-depth quote-mint guard; USDC/USDT/WSOL cannot be recorded as launches.
**Adjudication:** QUOTE_MINTS frozenset (transport.py:1498-1502) = USDC/USDT/WSOL. _map_new_token (1824-1834) and _map_migration (1920-1930) both drop a quote-mint-keyed event and increment events_skipped BEFORE LaunchEvent construction. store.py ShadowRecorder.observe imports the SAME frozenset from transport (store.py:590 — single source, no drift) and re-checks at 602-612, incrementing _orphan_events_total. Secondary guard verified. No action; keep the shared import.

### [GREEN] A-blockchain-core · ingestion — point-in-time (T-300a) + money discipline
**Issue:** CONFIRMED CORRECT. Event-time is on-chain block_time only; wall-clock is never substituted; money is integer.
**Adjudication:** block_time is sourced only from getTransaction blockTime (rpc path store.py:1300-1301 sets None if absent); when block_time_ms is None both _map_new_token (1837-1846) and _map_migration hold pending (_pending_no_block_time++/events_skipped++) and do NOT yield. wall_clock_ms is carried separately (1873) and data_staleness_ms is derived from block_time (1852). _time_now_s()/time.time() are used only for internal timeout tracking, never for event_time. Money: SOL->lamports int(float*1e9), token reserves int(); venue/tx_builder reject float money fields. Informational: sol_reserve_lamports briefly routes through float — fine at reserve magnitudes; parse via Decimal if PumpPortal ever emits high-precision SOL strings. No action.

### [GREEN] A-blockchain-core · execution — simulate-before-send + priority-fee math
**Issue:** CONFIRMED CORRECT. Simulate-on-every-submit holds, simulated bytes equal landed bytes, fee/CU math is integer-correct, idempotency poisoned only on confirmed fill.
**Adjudication:** execute() does build(real keys)->sign->simulate(4a)->size_cu->rebuild->sign->RE-simulate(4c) the exact final bytes->land (venue:283-333). _land_with_retry_entry fetches a fresh blockhash, rebuilds, re-signs and re-simulates on every retry, aborting on retry-sim revert (venue:889-931). DRY_RUN hard-stops before any network call in both land() (650-665) and the retry loop (859-875). _compute_priority_lamports=(cu_price*cu_limit)//1_000_000 integer floor (venue:1142/1147); size_cu_limit=int(Decimal(sim)*Decimal('1.25')) capped at 400_000 (tx_builder:98-109). Idempotency set added only when fill.landed (venue:342-343, exit 463-464). Versioned-tx note stands for LIVE: placeholder swap discriminator + malformed 8-byte System-transfer discriminator (tx_builder:262,272) + empty ALT list — moot in paper/stub, must be replaced with IDL-derived encodings before LIVE.

### [GREEN] A-blockchain-core · backtest — decision/outcome leak boundary + realizable-exit realism
**Issue:** CONFIRMED CORRECT (positive control the 7-finding set did not credit). Per-decision point-in-time boundary is load-bearing and tested; outcome-realism is conservative and anti-p-hacking-guarded.
**Adjudication:** outcome_harness.decide_entry and momentum_harness.decide_momentum_entry structurally separate DECISION (entry-time only) from OUTCOME (forward-only); each feature is stamped at its event-time and run through assert_features_leq_decision before any decision (outcome_harness.py:414-436; momentum cutoff = launch_block_time + T_ENTRY*1000, momentum_harness.py:403-498). The guard is genuinely load-bearing: tests inject a forward feature via the extra_features seam and assert LeakError end-to-end (tests/backtest/test_outcome_harness.py:133-173; test_momentum_harness.py:196-254). realizable_exit.py enforces realizable_net <= spot_net by construction (deduction>=0 + clamp), reads only post-decision marks, and its constants are a hash-frozen artifact (REALIZABLE_EXIT_PARAMS.frozen.json) that fails closed on drift. Asymmetric-trust in this lane is correct: pretrade_gate and gate_b are documented and structurally veto/measure-only (cannot size, widen a stop, add leverage, or override a hard stop).

### [GREEN] B-trading-safety · Circuit breaker + global kill switch
**Issue:** CONFIRMED correct and live-wired. Day-key derived from on-chain block_time_ms in event-time; integer lamports with explicit float rejection; asymmetric trust enforced by type; restart re-reads TRIPPED and never auto-resets; point-in-time restart seeding keys the persisted net into its own day.
**Adjudication:** Verified: PnLEvent rejects float on delta_lamports (circuit_breaker.py:119-122) AND block_time_ms (:124-126); _utc_day_key(block_time_ms) event-time keyed (:715, used :445); reset() requires a genuine OperatorResetToken and raises PermissionError otherwise (:521-538); trip_from_llm can only de-risk, no reset/size-up (:491-519); soft tier is monotone within a day and only lifted by an operator reset (:544-550); wired in __main__.py (record_pnl each tick via fast_loop.py:400-412, deferred flatten bridged :421, TRIPPED respected by SNIPE, kill switch on orchestrator + control plane). No change needed; the soft-tier-not-enforced-on-entry gap is tracked separately as Finding 4.

### [GREEN] B-trading-safety · Leak audit: gate_a / gate_b / baseline + harnesses (incl. realizable_exit)
**Issue:** CONFIRMED. No forward feature touches the decision. The harnesses structurally separate DECISION (<= T_ENTRY, leak-guarded) from OUTCOME (> T_ENTRY); the two functions never share an argument. realizable_exit only ever LOWERS outcome PnL (conservative invariant) and never touches the decision or leak boundary.
**Adjudication:** Verified: outcome_harness.py:414-436 assert_features_leq_decision raises LeakError on any feature whose event-time exceeds the decision cutoff; decision anchor is on-chain block_time (recv_wall_ms never a decision field). realizable_exit.py guarantees realizable_gross <= spot_gross by construction: deduction = spot_proceeds - int(spot_proceeds*multiplier) with multiplier in [0,1] (:370-375, clamped >=0) plus a final gross<=spot_gross clamp (:419-420); frozen params are hash-drift guarded (:201-211). 53 backtest tests pass. Only caveat is Finding 5's nominal-vs-true horizon stamping (a stamping optimism, not a structural decision/outcome leak).

### [GREEN] B-trading-safety · Circuit breaker + global kill switch
**Issue:** Day-key derived from on-chain block_time (event-time); integer lamports with float rejection; asymmetric trust enforced by type (trip_from_llm de-risk-only; reset requires an OperatorResetToken no automated/LLM path can mint); restart re-reads TRIPPED and never auto-resets. Wired live in __main__.
**Adjudication:** CONFIRMED GREEN. circuit_breaker.py:445 day = _utc_day_key(event.block_time_ms). trip_from_llm (491-519) can only force TRIPPED, never reset/widen. reset() (521-560) requires isinstance OperatorResetToken and raises PermissionError otherwise; on reset it clears _soft_reduced_days too (soft pause lifts only via operator re-arm). Soft-tier bookkeeping is monotone de-risk. Wired in __main__ (breaker built, record_pnl per tick via fast_loop, deferred flatten bridged, TRIPPED respected by SNIPE projection, kill switch on orchestrator + control plane). test_circuit_breaker_soft_tier 11 passed this session. No change; the soft-tier-not-on-entry gap is tracked separately.

### [GREEN] B-trading-safety · Leak audit: gate_a / gate_b / baseline + harness decision/outcome boundary
**Issue:** No forward feature reaches the decision: entry features stamped at decision block_time; assert_features_leq_decision raises LeakError on any feature event-time > cutoff; decision and outcome functions never share an argument; realizable_exit only lowers outcome PnL and reads outcome marks only; gate_b is a pure aggregator with a lower-95 bootstrap and a de-risk-only min-sample floor; no win-rate; float money rejected.
**Adjudication:** CONFIRMED GREEN (single-trade boundary). outcome_harness.py:392-436 assemble_decision_features stamps entry-only features at decision block_time and assert_features_leq_decision raises on any post-cutoff feature. gate_b.py TradeOutcome rejects float, carries only signed net PnL, no 'won' field; compute_gate_b_delta credits declines as 0, lower-95 bootstrap, min_sample hard-floor 10 (tighten-only, de-risk). realizable_exit.py:388-421 enforces realizable_gross = spot_gross - deduction(>=0) with a final clamp (conservative invariant) and reads only post-decision marks. Tests green this session (outcome_harness 12, run_edge_proof 5, realizable_exit 20). Caveat: the nominal-stamp optimism (finding #5). The structural single-trade leak boundary is sound; see the MISSED issue for the cross-trade / walk-forward gap not covered by this finding.

### [GREEN] C-platform-infra · risk / dead-man's switch (separate failure domain + asymmetric trust)
**Issue:** CONFIRMED verified-correct. dms_service.py is a true separate failure domain (own compose unit reading a cross-process heartbeat via the HeartbeatStore seam / Redis KV). Fail-closed on missing/unreachable heartbeat (_age_locked returns aging value on None, 259-263); fires on the conservative MAX of cross-process wall age and observed monotonic age (274) to defend against NTP steps; a stuck heartbeat (same seq) does not refresh the anchor (268); the FIRED latch is set BEFORE the venue submit (295 before 305); stand_down()/rearm() raise PermissionError unless handed a genuine DmsStandDownToken (319-324, 330-331) so the LLM/market/risk path cannot disarm it; Heartbeat.epoch_ms is int-enforced (86-87).
**Adjudication:** Verified by reading dms_service.py end-to-end. Minor note: t_dms_seconds<=0 raises ValueError in the constructor (216-217) rather than defaulting to 60s — the 'fail-closed to 60s on garbage' behavior lives in the dms_main.py env parsing, not the watchdog; verdict unchanged. This is the strongest control in the lane.

### [GREEN] C-platform-infra · telemetry / decision-log point-in-time integrity (T-300a)
**Issue:** CONFIRMED verified-correct. decision_log.py records event_time as {slot, block_time_ms} (124-127, on-chain event-time, never wall-clock); recorded_at_ms is a separate wall-clock field documented as audit-latency-only (100-102, 139); a recorded_at_ms < block_time_ms anomaly is logged, not silently dropped (107-120); no secrets/tokens are placed in the record.
**Adjudication:** Verified by reading decision_log.py:80-157. The append-only guarantee is delegated to the stdout->log-driver pipeline (146) with no in-process hash-chaining — acceptable for the design, worth stating only if tamper-evidence is later required.

### [GREEN] C-platform-infra · images/CI / no-secrets-in-images (+ CI gate 1)
**Issue:** CONFIRMED verified-correct with caveat. .gitignore:23-35 and .dockerignore:6-18 exclude .env/.env.* (keeping .env.example) plus *.key/*.pem/keypair*/wallet* patterns; Dockerfiles run non-root (controlplane adduser uid 1000). CI gate 1 (ci.yml:38-105) runs bandit + detect-secrets(--baseline) + a hard-fail grep for key patterns. CAVEAT (as auditor noted): ci.yml:82 detect-secrets audit --fail-on-unaudited is neutered by || true, and ci.yml:73,79 exclude .*\.json$ from detect-secrets wholesale.
**Adjudication:** Verified against .gitignore, .dockerignore, ci.yml. The caveat is a real residual gap: a Solana keypair is an int-array JSON; if named outside the keypair*/wallet* patterns it is neither gitignored, nor scanned (json excluded), nor caught by the grep gate (ci.yml:89-92 only matches PRIVATE_KEY=/SECRET=/sk-/PEM headers). Recommend dropping || true and narrowing the *.json exclusion. Gate 1 still holds on the belt-and-braces grep, so GREEN stands.

### [GREEN] C-platform-infra · risk / dead-man's switch (separate failure domain + asymmetric trust)
**Issue:** DMS is a true separate failure domain, fail-closed, non-disarmable by LLM/market/risk, NTP-step resistant.
**Adjudication:** CONFIRMED CORRECT. dms_service.py: watchdog reads Heartbeat only through injected HeartbeatStore seam (cross-process; never the writer's in-proc state, 129-157); unreachable/missing/malformed heartbeat -> read()=None -> age = mono_now - last_fresh (fail-closed, 259-263); age = max(cross-process wall age, observed monotonic age) defends NTP step (265-274); STUCK heartbeat (same seq) does not refresh the anchor (268-271); latch set BEFORE venue submit (295 before 305); stand_down()/rearm() raise PermissionError unless handed a genuine DmsStandDownToken (310-336) — the automated/LLM path cannot mint it (AC-046); t_dms_seconds<=0 raises ValueError (216); Heartbeat.epoch_ms float-rejected (86-87). tests/risk/test_dms_service.py (20) + property tests pass green this session. Honest caveat (not a defect): I did not independently re-verify the DryRunBlockEngineSubmitter no-transmit gating nor dms_main.py env fail-close-to-60 in this pass; trust-but-note. GREEN concurred.

### [GREEN] C-platform-infra · telemetry / decision-log point-in-time integrity (T-300a)
**Issue:** Decision log stamps EVENT-TIME (slot + block_time_ms), keeps wall-clock separate and audit-only, logs (not drops) a clock anomaly, leaks no secrets.
**Adjudication:** CONFIRMED CORRECT. decision_log.py: event_time = {slot, block_time_ms} (124-127), never wall-clock for the join; recorded_at_ms is a separate field documented as audit-latency-only and forbidden for backtest/label joins (32-37,100-102); recorded_at_ms < block_time_ms emits `decision_log_clock_anomaly` warning rather than silent drop (106-120); record dict (122-140) contains no tokens/keys. Point-in-time discipline (C-5) upheld. GREEN concurred; the 'append-only' guarantee is delegated to stdout->log-driver (no in-process hash-chain) as the finding notes — acceptable for the design.

### [GREEN] C-platform-infra · images/CI / no-secrets-in-images (+ CI gate 1)
**Issue:** Real .env untracked and excluded from images; Dockerfiles copy source only, non-root; signer distroless; CI runs bandit + detect-secrets + hard grep. One belt (detect-secrets) is thinner than the finding claims.
**Adjudication:** CONFIRMED with a sharpened caveat. .dockerignore excludes .env/.env.*/keypair/wallet patterns (7-18); .gitignore mirrors them (24-35); .env.example holds placeholders only. Dockerfile.controlplane COPYs aats/ + pyproject only, adduser aats non-root (31-32). REFINEMENT to the finding: it credits `detect-secrets scan --baseline` (ci.yml:76) with failing on NEW secrets — in detect-secrets 1.5.0 `scan --baseline` updates the baseline and exits 0; combined with `audit ... --fail-on-unaudited || true` (82) the detect-secrets portion of gate 1 is effectively NON-blocking. The real secret gate is the grep pattern-match (84-105, `exit $FAIL`) + bandit + untracked .env. That grep is narrow and `*.json` is excluded from detect-secrets (72,79), so a raw Solana keypair int-array named `id.json` (Solana CLI default, NOT in the .gitignore keypair*.json/wallet*.json patterns) would evade detect-secrets, evade grep, and evade .gitignore. No such secret is committed today, so overall posture stays GREEN, but the belt is thinner than stated.

### [GREEN] D-frontend-governance · Adversarial sentiment — asymmetric trust (Finding 3)
**Issue:** Manufactured hype can only lower conviction, never raise it, across news/velocity/caller sources.
**Adjudication:** CONFIRMED. pipeline.py:272-276 applies news only when mcs_delta<0 (positive news no-ops); news_scorer.py:215 clamps mcs_delta to [-1.0,0.0]. pipeline.py:317-320 applies velocity via apply_velocity_penalty_to_conviction (velocity.py:727-790) which raises ValueError (770,784 — NOT assert, survives -O) on a forged negative penalty plus defensive clamp (779-780) plus post-condition raise. caller_score.py:908 forces mcs_delta_contribution=min(0.0,…); assert_caller_signal_cannot_raise_conviction (984,998) is ValueError+clamp+post-condition. Belt-and-suspenders verified.

### [GREEN] D-frontend-governance · Telegram command channel (Finding 4)
**Issue:** ControlPlaneClient is de-risk-only by shape: four methods (get_status read; kill/flatten_mint/pause de-risk), pause posts a hard-coded SHADOW target not a caller arg.
**Adjudication:** CONFIRMED. control_plane_client.py:44 PAUSE_TARGET_MODE='SHADOW' module constant; Protocol (84-95) exposes exactly get_status/kill/flatten_mint/pause — no size-up/widen/mode-up/breaker-reset method exists. Money fields carried verbatim as int lamports / decimal-string (StatusSnapshot 62-67), no win-rate field, token redacted via redact_token (imported line 36, documented 24-27). Shape-level enforcement verified.

### [GREEN] D-frontend-governance · FAST-loop risk rule engine (Finding 5)
**Issue:** No override adds risk: DeRiskIntentFactory has no EntryIntent path; strict short-circuit priority; TP only reduces; float money rejected.
**Adjudication:** CONFIRMED. rule_engine.py:211-254 evaluates narrative_failure (pre-set flag, never calls LLM) then hard_stop then take-profit, each returning on first fire so a full exit is never overridden by TP. _scale_out (276-298) uses fraction_bps bounded [1,9999] (TakeProfitLevel.__post_init__ 124-127); _full_exit uses _FULL_EXIT_BPS. Factory has no EntryIntent constructor (comment 256; type note 179-198). Float money raises TypeError (92-101,118-123,162-165). Verified correct.

### [GREEN] D-frontend-governance · Loop governance (L1→L2→L3) + control-plane server safety (Finding 6)
**Issue:** Loop pinned at L2 with capital hard-denylisted; server fences LIVE, makes risk-config tighten-only, gates breaker reset on TRIPPED+operator (no LLM path).
**Adjudication:** CONFIRMED with one correction. LOOP.md:13 pins L2; denylist (22-29) forbids flipping DRY_RUN, mainnet tx/signing, editing .env/secrets, editing frozen contracts, deleting safety primitives; graduation (61) keeps capital human-gated 'regardless of level'. server.py:877-928 risk-config is tighten-only (403 risk_increase_rejected on widen); breaker reset (1013-1086) gated on TRIPPED + _check_operator_auth, LLM has no reset path. CORRECTION to the auditor's rationale: the claim that a real-mode dashboard LIVE request '403s' because postJSON omits X-CEO-Auth is imprecise — it 401s at _check_operator_auth (postJSON also omits the operator Bearer, api.ts:157-165) before reaching the CEO gate, and is CORS-blocked before that. The safety conclusion (dashboard cannot enable LIVE) still holds, but for a different reason — see the missed RED finding below.

### [GREEN] D-frontend-governance · Adversarial sentiment (asymmetric trust)
**Issue:** CONFIRMED correct: manufactured hype can only LOWER conviction across all three sources. news_scorer.py:215 clamps mcs_delta to [-1.0,0.0]; velocity mcs_penalty is >=0 and applied subtractively; caller mcs_delta_contribution is <=0 (caller_score.py:908). apply_velocity_penalty_to_conviction (velocity.py:727-791) and assert_caller_signal_cannot_raise_conviction (caller_score.py:942-1006) each enforce with an explicit `raise ValueError` (NOT assert, survives python -O) for a forged positive delta, PLUS a defensive clamp, PLUS a post-condition raise if adjusted>base_conviction. Point-in-time preserved: tier_a.py:462-463 filters event_time_ms<=decision_time_ms as the FIRST operation, and news_scorer.py:171-175 re-filters defensively.
**Adjudication:** Read every clamp/guard and the point-in-time filter path end to end. The load-bearing ValueError-not-assert hardening and belt-and-suspenders clamp are present in both helpers and wired into pipeline stages 5 and 7. No refutation found. GREEN upheld.

### [GREEN] D-frontend-governance · Telegram command channel
**Issue:** CONFIRMED de-risk-only by shape: ControlPlaneClient exposes exactly four methods (get_status read; kill / flatten_mint / pause de-risk). pause() posts module-constant PAUSE_TARGET_MODE='SHADOW' (control_plane_client.py:44,287-291) — destination is never a caller argument, so no caller can move mode UP, reset the breaker, or enable LIVE via this client. Money carried verbatim; _status_from_wire:181-185 rejects non-int lamports (float); operator token lives only in the httpx adapter and is routed through redact_token on every error path (250-254). No win_rate field.
**Adjudication:** Read the full client. The four-method protocol shape and the hard-coded downward pause target are the enforcement, and they hold. GREEN upheld.

### [GREEN] D-frontend-governance · FAST-loop risk rule engine
**Issue:** CONFIRMED no override bypasses the safety spine: HierarchicalRiskRuleEngine holds a DeRiskIntentFactory whose only methods are exit/reduce/veto — there is NO entry/size-up constructor (intents.py:258-317), so a risk-increasing outcome is not an expressible value. Strict short-circuit priority NARRATIVE_FAILURE=1 > HARD_STOP=2 > TAKE_PROFIT=3 (rule_engine.py:70-79,211-254). LLM reaches the FAST loop ONLY as a pre-set narrative_failure boolean (force-exit, de-risk); the loop never awaits the LLM. Hard stop fixed-at-entry (never widened here); TP fraction_bps constrained to [1,9999]; float money rejected via _to_decimal_price.
**Adjudication:** Read the engine and the intents factory. The type-level construction (no EntryIntent path) is the correct enforcement and is intact. GREEN upheld.

### [GREEN] D-frontend-governance · Loop governance (L1->L2->L3) + control-plane server safety
**Issue:** CONFIRMED: LOOP.md pins L2 with capital HARD-DENYLISTED (lines 13,21-29) — forbids flipping DRY_RUN_ENABLED, any mainnet tx/signing, editing .env/secrets, editing frozen contracts, deleting safety primitives — and 'Capital/outward actions remain human-gated regardless of level (never graduates)' (line 61). Server: /api/risk-config is tighten-only with 403 on widen (server.py:877-928); /api/breaker/reset gated on TRIPPED + operator auth with NO LLM/automated reset path (1016-1086, LLM may TRIP never reset); money is int lamports/Decimal-string; no win_rate field. Dashboard postJSON never sends X-CEO-Auth, so a real-mode LIVE request 403s.
**Adjudication:** Read LOOP.md, the breaker-reset and risk-config endpoints. All claims hold. One caveat folded into the MISSED issue below: the same absent-header fact the finding cites as a LIVE-safety win also means the dashboard sends NO operator Bearer token, which the finding did not surface. GREEN upheld for the stated scope.

### [GREEN] E-security-edge · Key isolation + secret hygiene
**Issue:** CONFIRMED correct: no wallet private key in code/env/logs; hot core holds pubkey only; secret is a Vault path reference.
**Adjudication:** .env.example:10-21 structurally removes WALLET_PRIVATE_KEY/KEYPAIR_JSON by design; WALLET_PUBKEY only for hot core (line 46); secret is a Vault path (WALLET_SECRET_VAULT_PATH, line 48) fetched only by aats-signer. signer_client.py:39-40 forces log level WARNING and never logs tx bytes. Consistent with the companion audit's history-aware secret sweep (clean). The signer holds no key today. Recommend automating gitleaks+trufflehog over tree AND full history as a durable CI gate (audit C9).

### [GREEN] E-security-edge · Edge-proof harnesses (outcome/momentum/run_edge_proof, realizable-exit)
**Issue:** CONFIRMED leak-free and fail-closed; verified by reading the code AND running the tests (37/37 pass).
**Adjudication:** DECISION and OUTCOME are structurally separate functions that never share the forward path; assert_features_leq_decision raises LeakError BEFORE any decision (outcome_harness.py:414-436). T-300a honored: decision anchors on on-chain block_time; recv_wall_ms is monitoring-only (EntryRecord line 316); momentum uses a correct moving cutoff = block_time + T_ENTRY*1000 (momentum_harness.py:403-409). Fail-closed: EXIT_FAIL_CLOSED(2) on zero resolved (run_edge_proof.py:183-185); _NullBlockTimeResolver censors every record with no RPC (80-90); GO requires BOTH gate_a_model AND gate_b (191). Money is integer lamports/Decimal; no win-rate (asserted by test). All three frozen artifacts (baseline/momentum/realizable) have frozen_hash populated; realizable net <= spot enforced by construction + clamp (realizable_exit.py:417-421). I ran tests/backtest/test_realizable_exit.py + test_outcome_harness.py + test_run_edge_proof.py = 37 passed; the leak RED-tests (test_leak_guard_red_*) and the drift guard (test_frozen_hash_fails_on_drift) genuinely fail if the control is removed, and a GO verdict is reachable (test_verdict_go_on_winner_heavy_corpus) so the NO-GO is not rigged. Keep the frozen-hash drift tests in CI.

### [GREEN] E-security-edge · Launch-data edge verdict (n=4187)
**Issue:** CONFIRMED genuine NO-GO with no regression or fabricated GO.
**Adjudication:** EDGE-PROOF-momentum-DECISIVE-2026-07-06.md documents the honest n=497 GATE-B PASS reversing to NO-GO on n=4,187 real launches (GATE-B +0.041 -> -0.011; GATE-A model FAIL). The realizable exit model only makes outcomes more conservative (realizable <= spot, proven by construction + the invariant tests), so it cannot manufacture a GO; the GO path being reachable in tests confirms the verdict machinery is honest, not hardwired. Both on-chain-launch strategies are decisively NO-GO; capital stays disabled. Any future re-run must keep exit_model=realizable (default) and both gates.

### [GREEN] E-security-edge · REACTION-CORPUS-SPEC (reaction/smart-money pivot)
**Issue:** CONFIRMED leak-safe as specified; correctly flagged as an interface spec whose leak boundary must be enforced by B's future harness.
**Adjudication:** REACTION-CORPUS-SPEC.md: decision anchor = signal_block_time_ms (on-chain, T-300a explicit 'NEVER wall-clock; record None+censor if unavailable', lines 33,43-44); decision uses only signal fields + strictly-pre-signal context (44-45); outcome = forward path strictly after the signal, entry price haircut for latency/slippage (46); money int lamports/Decimal net of ~6% (47). Asymmetric trust honored: source_prior 'SELECTIVITY ONLY, de-risk' (36), baseline = follow-EVERY-signal (50), model = quality-filtered SUBSET (51), 'NEVER fabricate a GO' (53). This is a spec, not built code — line 42 states the leak boundary must be enforced by B's harness when built. No leak in the spec itself. When B builds it, reuse assert_features_leq_decision against signal_block_time_ms and clamp source_prior <= neutral.

### [GREEN] E-security-edge · Key isolation + secret hygiene — .env.example / signer_client.py
**Issue:** CONFIRMED at spot-check level. .env.example structurally removes WALLET_PRIVATE_KEY/KEYPAIR_JSON by design (ADR-0009 §5.3, header hard-rule block); signer_client forces _LOG_LEVEL=WARNING and never logs tx bytes/pubkeys; grep for PRIVATE_KEY/secretKey across the non-md tree hit only benign locations (ci.yml scanner regex, test fixtures, and a memecoin-bot Go COMMENT referencing SOLANA_PRIVATE_KEY — not a literal). The hot core holds pubkey only.
**Adjudication:** Control is correct for the current paper state. NOTE: full secret hygiene (git-history sweep, Redis write paths, wallet safety) is primarily crypto-security's lane; I verified the tracked tree by spot-grep, NOT a full history-aware gitleaks/trufflehog sweep — that automation remains a recommended CI gate.

### [GREEN] E-security-edge · Edge-proof harnesses (launch + momentum) — outcome_harness / momentum_harness / realizable_exit / gate_a / gate_b
**Issue:** CONFIRMED leak-free and fail-closed for launch+momentum. DECISION and OUTCOME are structurally separated (decide_entry/decide_momentum_entry take no forward/price/outcome arg; a test asserts this by signature). Every decision feature is a PITFeature stamped at its event-time and run through assert_features_leq_decision BEFORE any decision; a RED test injects a forward mark via the extra_features seam and asserts LeakError both unit and end-to-end. Decision anchors on on-chain block_time (T-300a); recv_wall_ms is monitoring-only; momentum uses a correct MOVING cutoff = block_time + T_ENTRY*1000. Fail-closed: _NullBlockTimeResolver censors every record with no RPC, unresolvable/malformed records are CENSORED not fabricated, empty set -> EXIT_FAIL_CLOSED(2), GO requires gate_a_model AND gate_b. Money is integer lamports/exact Decimal; TradeOutcome rejects float; no win-rate. Realizable-exit invariant (realizable net PnL <= spot) holds by construction + clamp. Frozen hashes populated and VERIFY at import (baseline e1e7..., momentum 03bc..., realizable 53b5...). 81 backtest tests pass this session.
**Adjudication:** Point-in-time discipline, cost-net accounting (~6% round trip: tip+priority+entry slip+AMM+exit slip+adverse), model⊆baseline (de-risk-only selectivity), and anti-p-hacking freezes are all real and tested. Model decisions are documented THIN placeholders (not the trained classifier) — correctly labeled. Two honestly-disclosed (not hidden) limitations noted: momentum ~64s timing-drift latency-optimism, and the 150bps adverse-selection cost floor is UNCALIBRATED (C-11); neither can rescue a GO in the current NO-GO state.

### [GREEN] E-security-edge · Launch/momentum edge verdict (n=4187) — EDGE-PROOF-momentum-DECISIVE-2026-07-06.md
**Issue:** CONFIRMED genuine NO-GO, no fabricated GO. Report shows the n=497 GATE-B PASS (+0.041, lower95 +0.026) REVERSED at n=4,187 to delta -0.011 (lower95 -0.060) with GATE-A model FAIL (-0.219/SOL, lower95 negative). Both on-chain-launch strategies decisively NO-GO; capital stays disabled.
**Adjudication:** This is the edge proof working (a small-sample false positive killed by 8x more data), not a regression or fabrication. The default realizable exit only makes outcomes more conservative, so it cannot manufacture a GO. Verdict is sound.

### [GREEN] E-security-edge · REACTION-CORPUS-SPEC (the spec document) — .agency/04-plan/REACTION-CORPUS-SPEC.md
**Issue:** CONFIRMED leak-safe AS A SPEC. Decision anchor = signal_block_time_ms (on-chain, T-300a, 'NEVER wall-clock; record None+censor'); decision may use only signal fields + strictly-pre-signal context; outcome = strictly-post-signal forward path; entry price haircut for latency/slippage; money int lamports/Decimal net of ~6%; source_prior labeled 'SELECTIVITY ONLY, de-risk'; baseline = follow-every-signal; model = quality-filtered subset; 'NEVER fabricate a GO.'
**Adjudication:** The spec's leak boundary is sound. HOWEVER this verdict is now STALE: the spec explicitly deferred enforcement to 'B's harness when built', and that harness has since been built and wired into the runnable proof — see MISSED issue on reaction_harness.py, which the prior audit did not review.
