# AATS dev/test dependencies — installed in CI but NOT in production images
-r requirements.txt

pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0
ruff==0.4.10
mypy==1.10.0
types-redis==4.6.0.20240417
bandit==1.7.9       # secret-scan / security linting
detect-secrets==1.5.0  # leak-guard hook (placeholder; CI fills body)
