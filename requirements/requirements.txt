# AATS Python requirements — pinned for reproducibility.
# Generated: 2026-06-16
# Python: 3.11
#
# HARD RULE: no real keys, secrets, or credentials here.
# See .env.example for the environment variable schema.
#
# The snipe+fast HOT PATH is Rust (see rust/); these packages are for the
# SLOW loop, models, control plane, DMS, and Telegram service.

# ---------------------------------------------------------------------------
# Telemetry
# ---------------------------------------------------------------------------
prometheus-client==0.20.0
structlog==24.1.0

# ---------------------------------------------------------------------------
# Config / env
# ---------------------------------------------------------------------------
python-dotenv==1.0.1
pydantic==2.7.1
pydantic-settings==2.3.4

# ---------------------------------------------------------------------------
# Control plane API
# ---------------------------------------------------------------------------
fastapi==0.111.1
uvicorn[standard]==0.29.0
httptools==0.6.1
websockets==10.4
python-multipart==0.0.9

# ---------------------------------------------------------------------------
# Redis message bus + KV
# ---------------------------------------------------------------------------
redis==5.0.6

# ---------------------------------------------------------------------------
# Solana slow-loop / tooling (NOT the hot path)
# ---------------------------------------------------------------------------
solders==0.21.0
solana==0.35.0
anchorpy==0.20.1

# ---------------------------------------------------------------------------
# HTTP (enrichment, Jito REST)
# ---------------------------------------------------------------------------
httpx==0.27.0
tenacity==8.3.0
anyio==4.4.0

# ---------------------------------------------------------------------------
# gRPC (Geyser/Yellowstone)
# ---------------------------------------------------------------------------
grpcio==1.64.1
grpcio-tools==1.64.1
protobuf==5.27.1

# ---------------------------------------------------------------------------
# ML / model training + ONNX export (inference is RUST-side)
# ---------------------------------------------------------------------------
lightgbm==4.3.0
onnx==1.16.1
onnxruntime==1.18.0
scikit-learn==1.5.0
pandas==2.2.2
numpy==1.26.4
# Parquet engine for pandas.read_parquet — required ONLY by
# ParquetCallerOutcomeStore (aats/sentiment/caller_score.py, EN5) when
# CALLER_OUTCOME_STORE_PATH is configured; lazily imported so a deployment
# that never configures it does not need this at import time.
pyarrow==17.0.0
# LightGBM -> ONNX export (T-310 snipe classifier). Export-time only; inference is Rust-side.
onnxmltools==1.16.0
skl2onnx==1.20.0

# ---------------------------------------------------------------------------
# NLP / LLM (SLOW loop ONLY — never on FAST/SNIPE path)
# ---------------------------------------------------------------------------
openai==1.30.5
instructor==1.3.3
tiktoken==0.7.0

# ---------------------------------------------------------------------------
# Telegram operator channel
# ---------------------------------------------------------------------------
python-telegram-bot==21.3

# ---------------------------------------------------------------------------
# Telegram MTProto ingestion (EN3 — MCS sentiment pipeline, SLOW loop only)
# Lazily imported ONLY inside HttpTelegramMTProtoClient at runtime with real
# creds; never imported at module load time and never required by the test
# suite (aats/sentiment/adapters.py always uses the offline fake in tests).
# ---------------------------------------------------------------------------
telethon==1.36.0

# ---------------------------------------------------------------------------
# Dev / test (install in CI with pip install -r requirements/requirements-dev.txt)
# ---------------------------------------------------------------------------
# (moved to requirements-dev.txt to keep the runtime image lean)
